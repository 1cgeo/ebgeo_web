# Persistência durável dos demais produtores

Status: em obra. Prioridade: bloqueia lançamento. Depende da [compatibilidade](01-compatibilidade.md). A primeira onda do bloco B4 fechou posição, mapa-base e as chaves de atlas (ordem de mapas, cores de crachá, aparência e registro de ícones); a tabela abaixo diz, família por família, o que ficou.

## Estado e pontos de entrada

Feições e comentários já possuem diário; briefings/slides e catálogo foram ampliados e validados nesta execução. Não basta encontrar uma chamada de logging: ela precisa anteceder a gravação e ter falha propagada. A fronteira comum está em `frontend/src/js/store/store-transaction.js`, `frontend/src/js/store/sync/operation-dispatcher.js`, `frontend/src/js/store/sync/queue-journal.js` e `frontend/src/js/store/document-lock.js`.

| Família | Arquivos a revisar | Lacuna ou cuidado |
| --- | --- | --- |
| Mapas | `frontend/src/js/store/map.operations.js`, `frontend/src/js/store/repositories/index.js` | Criação, renomeação e remoção continuam gravando antes do log (ondas 6 e 7 do bloco B4); exclusão e renomeação abrangem documentos auxiliares. A COR DE CRACHÁ que as duas escrevem já é write-ahead (commit "atlas: ordem de mapas, cores de crachá e aparência registram a intenção antes de gravar"), e ela carrega a decisão que a migração das duas precisa respeitar: `setMapBadgeColors` abre a PRÓPRIA transação, e isso só é seguro porque os cinco chamadores a invocam FORA de qualquer seção de `withMapDocument`. A fila de `document-lock.js` é FIFO e sem reentrância, então quem migrar renomear ou excluir tem de mover a intenção da cor para o `tx` do pai em vez de aninhar uma segunda transação, e o sintoma de errar é a interface PENDURADA, não um teste vermelho |
| Posição e mapa-base | `frontend/src/js/store/map.operations.js` | Persistem antes do log. A metade DESTRUTIVA foi fechada em 2026-09-13 (achado F1, bloco B1, commits "sync: exclusão de subtipo de mapa é recusada por operação, não aplicada ao mapa" e "mapa: limpar posição vira atualização com campos nulos, com gate e trava do mapa alvo"): limpar a posição emitia `mapPosition` DELETE, que no servidor é exclusão DO MAPA, porque a op de configuração carimba o id do mapa como `entityId` e o caminho de exclusão não lia o subtipo. O servidor passou a recusar esse envelope por operação (`mapSubtypeDeleteDenialReason`) e `clearMapPosition` passou a emitir UPDATE com os cinco campos nulos, inclusive no caso legado sem id, que antes não emitia op nenhuma. Ela ganhou o gate `checkPermission(GuardAction.UPDATE_MAP)` que as duas irmãs já tinham, e as três passaram a perguntar a trava do mapa ALVO pelo disco (`isMapLocked`) em vez do conjunto em memória do mapa corrente. FECHADA TAMBÉM A METADE DE DURABILIDADE, em 2026-09-13 (onda 1 do bloco B4, commit "mapa: mapa-base e posição registram a intenção antes de gravar"): `setBaseLayer`, `updateMapPosition` e `clearMapPosition` registram a intenção por `tx.recordOperation` dentro de `runTransaction`, com a trava do documento por fora e a gravação do documento como função de persistência DEVOLVIDA, de modo que falha de diário impede a entidade e falha de entidade deixa intenção recuperável. As três não tinham efeito de memória nem evento, então nada foi para `tx.deferSync`. Junto entrou `assertRemoteMapIdentity`, o guarda que o catálogo já tinha: o fallback de compatibilidade responde documento VAZIO para mapa ausente, e gravá-lo de volta criaria um mapa fantasma com id que o servidor nunca emitiu. Guarda: `frontend/tests/integration/map-settings-write-ahead.test.js` |
| Camadas | `frontend/src/js/layers/layer.manager.js`, `frontend/src/js/store/layer.operations.js` | Debounce e memória podem anteceder a intenção durável |
| Grupos e membros | `frontend/src/js/tool_manager/group_manager.js`, `frontend/src/js/store/group.operations.js` | Registrar mudanças de membros e composição, não somente o documento do grupo |
| 3D e 360 | `frontend/src/js/store/cesium3d.operations.js`, `frontend/src/js/store/streetview360.operations.js` | Gravação anterior ao log, caminhos de importação/lote e callbacks sem cobertura uniforme |
| Configurações | `frontend/src/js/store/settings.operations.js`, `frontend/src/js/store/temporal.operations.js`, `frontend/src/js/store/atlas-appearance.service.js`, `frontend/src/js/store/customIcons.operations.js`, `frontend/src/js/store/atlas-setting-target.js` | Notas, grade e temporal já usavam transação. As CHAVES DE ATLAS foram fechadas em 2026-09-13 (onda 1 do bloco B4, commits "atlas: ordem de mapas, cores de crachá e aparência registram a intenção antes de gravar" e "ícone personalizado: só o miolo entra na transação, com o upload fora"): ordem de mapas, cores de crachá, aparência do projeto e registro de ícones registram a intenção antes da gravação local, e o `entityId` da op saiu do dispatcher para `resolveAtlasSettingId`, porque id que não seja UUID nem a sentinela `atlas` é descartado antes da fila e é ele que decide se a intenção é durável. TRÊS DECISÕES QUE UMA REESCRITA DESATENTA PERDE. A permissão em ordem e cores gateia só a OP, nunca a escrita local: as duas chaves também são preferência de visão, e `getMapBadgeColor` grava cor na primeira pintura de um crachá, então recusar a escrita custaria um aviso num caminho passivo; o que deixou de acontecer é enfileirar a op que o servidor responde com 403 e que travava a fila inteira. O `try/catch` que engolia tudo na aparência saiu: o retorno `false` ficou só para patch vazio e permissão recusada, e falha de persistência PROPAGA com `STORE_PERSIST_ERROR`, que tem ouvinte com aviso na tela e relato; nenhum chamador lia o `false`, e `_handleSave` (`frontend/src/js/modals/atlas-settings.modal.js`) dizia "Configurações salvas." depois de uma quota. Aquele `_handleSave` ficou SEM tratamento próprio da exceção, que é pendência de quem tocar o modal. No ícone personalizado só o miolo entrou na transação: o upload e o blob local não são reversíveis pelo diário e rodam antes, com compensação manual no `catch`, e o cache de módulo passou a andar em `tx.deferSync`. Continua em aberto ali o par intenção/blob quando a gravação falha e o id é local, que é a fila durável de blobs do bloco B8. Guardas: `frontend/tests/integration/atlas-keys-write-ahead.test.js`, `frontend/tests/integration/customIcons.operations.test.js` e a parte C de `frontend/tests/integration/settings-operations-real.test.js`, que cobra da porta write-ahead o mesmo recorte de id que o caminho antigo já tinha |
| Trava do mapa | `frontend/src/js/locking/map-lock.controller.js` | Conferir intenção durável e mapa de destino no caminho completo da UI |

## Implementação

1. Para cada produtor, listar criação/edição/exclusão, importação, cópia, lote, undo/redo e autosave. Verificar o debounce do editor de briefing, mesmo com o store já protegido.
2. Capturar escopo, época e IDs finais antes do primeiro await; ler e preparar sob a trava correta. Registrar a intenção completa antes de alterar a entidade. Evitar criar uma referência remota para um mapa inexistente pelo fallback de compatibilidade.
3. Publicar para envio somente após materialização confirmada. Recuperar a intenção preparada com os mesmos IDs; não duplicar undo, entidades ou efeitos visuais. Manter falha de quota visível e propagada.
4. Preservar dependências entre mapa, camada, grupo, recurso e entidade. Uma criação recusada bloqueia seus dependentes, sem travar trabalho independente. Tratar alterações de vários documentos junto aos [comandos compostos](04-comandos-compostos.md).

## Aceite

Por família, falhar antes/depois do diário, na gravação da entidade e na marca de materialização. Reabrir e comparar conteúdo completo, referências e IDs. Forçar concorrência na mesma entidade, mapas de destino diferentes e troca de escopo durante await. Nenhuma edição aceita pode ficar sem resultado confirmado ou intenção recuperável. Incluir posições antigas somente com campos planos. Usar como referência os testes reais de diário de briefing e catálogo, sem substituir asserções de disco por mocks de logging.
