# Livro-razão de correções

Espelho das **correções**, não dos sucessos: uma linha por vez em que um desvio foi pego, seja por você, seja pela realidade (teste que quebrou, bug que voltou, produção que reprovou). Existe para que a retrospectiva e o agente **percebam padrão e recorrência** ao longo do tempo, não para virar placar.

É memória lida por raciocínio, **nunca métrica a maximizar**: no instante em que se otimiza este arquivo, a armadilha mecânica voltou. Registro de sucesso premiaria covardia (fazer menos para o número subir); registro de correção alimenta aprendizado. A correção é sinal de primeira classe, presente, não falta.

Doutrina: [`docs/doutrina.md`](doutrina.md), princípios 1, 2, 5 e 6.

## Como usar

- **ESCREVER** (qualquer sessão, no momento da correção): uma linha por evento, append-only. Não é toda ida e volta, só o **desvio real**. O conserto curado vai para o learnings da skill, a regra ou o teste; aqui fica só o evento, para a recorrência ficar visível — que o learnings, curado e podado, descarta.
- **O QUE NÃO ENTRA: achado de produto.** Bug encontrado no código (um IDOR, uma corrida, um limitador inefetivo) vai para o relatório de bugs, nunca para cá. Aqui só entra desvio de **como o trabalho foi conduzido**: o que eu afirmei sem verificar, o teste que não prendia, a premissa que inventei. A distinção não é purismo de arquivo, é o que mantém o sinal legível: a poda de 2026-07-25 tirou doze linhas de taxonomia de bug que somavam 14% dos eventos e não diziam nada sobre hábito nenhum (ver a síntese abaixo). Se a linha não descreve algo que eu faria de novo, ela não pertence a este arquivo.
- **LER** (na retrospectiva): alguma classe recorreu? Recorrência na **mesma** skill significa que o learnings não pegou: mudar a abordagem, não só re-anotar. Recorrência **entre** skills é lacuna de doutrina ou de constituição: subir, não tratar caso a caso.
- **PODAR** (na retrospectiva): classe resolvida e sem reincidência vira uma linha de síntese e os eventos crus saem.

## O gate que evita o teatro

Uma entrada só vale se responder **onde a lição foi codificada**. Sem isso vira anedota acumulada, o modo de falha clássico do postmortem que ninguém lê. Em software o gancho tem forma preferencial:

1. **teste de regressão** (o mais forte: falha se a lição for esquecida),
2. regra na constituição ou em `.claude/rules/`,
3. learning da skill,
4. página de wiki ou decisão registrada.

Correção sem nenhum dos quatro é sinal de que ela não foi entendida ainda.

## Duas fontes (a da realidade é o ouro)

- `sessao` — o desvio foi pego no meio da tarefa. Rápido, mas é julgamento na hora.
- `realidade` — o mundo rendeu o veredito: teste quebrou, controle negativo mostrou que o teste não prendia nada, bug voltou, comportamento no ambiente contradisse o esperado. Verdade-terreno; é o dado externo que sustenta o laço (princípio 2).

## Vocabulário de classes (extensível)

`doc-sobre-codigo` (afirmou a partir da prosa que descreve o código, em vez do código real) · `eco-de-sessao` (repetiu conclusão de sessão anterior sem reconferir) · `verificacao-fantasma` (deu como verificado o que não foi; assumiu que um comando fez efeito sem conferir) · `teste-que-nao-prende` (teste passa com e sem o fix; faltou controle negativo) · `escopo` (fez mais ou menos que o pedido) · `premissa-inventada` (agiu sobre premissa que não foi dada nem confirmada) · `aprovacao-presumida` (tratou esclarecimento de escopo como autorização para ação específica) · `estado-como-fato` (gravou na memória durável algo que é estado efêmero) · `default-irreal` (default que só funciona por acidente do ambiente de dev) · `regressao-propria` (o fix introduziu o defeito seguinte)

## Sínteses (classes resolvidas)

Classes resolvidas e sem reincidência posterior, condensadas dos eventos crus. Toda classe com qualquer recorrência (ou 2+ eventos) permanece crua abaixo.

- **Achado de produto não é desvio de trabalho (12 eventos removidos, 2026-07-19).** A leva de auditoria do backend registrou aqui doze linhas com taxonomia de BUG (`idor` ×2, `credencial-em-log` ×2, `race`, `rate-limit-inefetivo`, `validacao`, `permissao-invertida`, `crash-uncaught`, `invariante-violado`, `lacuna-adjacente`, `sucesso-falso`). Elas descreviam defeitos do PRODUTO, não desvios de como o trabalho foi conduzido, e viveram íntegras num `bugs-backend.md` na raiz, com 117 achados, cada um com mecanismo, correção do verificador e nota de fix. Esse arquivo foi podado em 2026-08-14, depois de conferido item a item contra o código: a auditoria do backend já tinha codificado tudo em código, teste e wiki, e o único item ainda aberto de propósito foi para [`wiki/config-dinamico.md`](wiki/config-dinamico.md). Eram 14% dos eventos e nenhuma dizia nada sobre um hábito: diluíam exatamente o sinal de recorrência que este arquivo existe para dar. Codificado na regra de ESCREVER acima, para não voltar.
- **`aprovacao-presumida` (1 evento, 2026-07-18).** Rodei `git merge origin/main` depois de perguntar se devia; a resposta reafirmava o escopo e não aprovava o merge. Esclarecimento de escopo não é autorização, e a lição virou linha da constituição (princípio 4, "Confiança é gradiente"), que é o gancho mais forte que ela podia ter.
- **`default-irreal` (1 evento, 2026-07-18).** `SEARCH_API_URL` e `SV360_SERVICE_URL` apontavam para portas que só funcionavam por acidente do proxy do Vite. Default que depende do ambiente de dev mente sobre o contrato; hoje as duas bases saem do `/api/config` e o tema tem página própria ([[config-runtime-urls-relativas]]).
- **`eco-de-sessao` (1 evento, 2026-07-18).** Agi sobre nota de memória que mandava migrar de `pollPeerFeature` para os deterministic-waits do SyncLedger; no código, a migração já estava feita. Nota de memória descreve o instante em que foi escrita: reconferir contra o código antes de agir virou o aviso padrão que acompanha toda memória recuperada.
- **`gate-inalcancavel` (1 evento, 2026-07-19).** Subi o merge de mapas de `write` para `manage` no servidor e a proteção não protegia nada, porque este cliente nunca chama `POST /maps/:id/merge` (combina localmente e sincroniza como ops comuns). Gate no servidor sem chamador é teatro: o gate real foi para `map.manager.combineSelectedMapsIntoTarget`, e a dupla foi verificada de novo em 2026-07-25.
- **`risco-aceito` (1 evento, 2026-07-19).** O furo de autodeclaração de OM no cadastro ficou aberto por decisão explícita. Em vez de deixar sem teste, o caso `KNOWN GAP` AFIRMA o comportamento inseguro atual, então o dia em que alguém fechar o furo o teste cai e obriga a decisão a ser revista. Risco aceito sem teste é risco esquecido.
- **`assimetria-entre-superficies` (1 evento, 2026-07-25).** `requireAtlasPermission` e o `enforceProjectReadable` do sv360 respondiam 403/404 em ordens opostas para o mesmo caso. O defeito não era o vazamento, que é pequeno, era a INCONSISTÊNCIA entre dois módulos do mesmo backend: é ela que faz o próximo módulo copiar o padrão errado. Escada única implementada, com `atlas-404-vs-403-escada.test.js`.

## Recorrências vivas (índice dos eventos crus)

Classes que **recorreram** e por isso continuam cruas abaixo, ao contrário das sínteses. Este índice
existe porque a recorrência é o produto deste arquivo e ela estava ilegível: para vê-la era preciso
ler 79 linhas e contar à mão. Cada bloco diz quantas vezes, em que datas, e qual foi a mudança de
ABORDAGEM (não a re-anotação), como manda a regra de PODAR.

- **Conferir um subconjunto e tratar como o conjunto (9 eventos: 2026-07-18, 07-24, 07-25 ×3, 08-14 ×2, 08-18 ×2).**
  `grep` em dois arquivos da raiz, `grep -v` que excluía o próprio alvo, `lint`/`test`/`test:coverage`
  da raiz delegando só para o frontend, `npm test -- <a> <b>` rodando só o primeiro padrão, o E2E que
  não estava no comando prescrito, e o inventário de cobertura montado a partir de uma lista de
  diretórios escrita à mão. Abordagem atual, depois da terceira recorrência: parar de consertar o
  caso e afirmar a PROPRIEDADE em teste (`frontend/tests/unit/scripts-da-raiz.test.js`), e derivar
  todo inventário do versionamento (`git ls-files`), nunca de enumeração. **Os dois eventos de
  08-18 corrigem essa própria abordagem em direções opostas**, e valem como aviso: derivar do
  versionamento deixa o guarda cego ao arquivo ainda não rastreado (a correção é
  `--cached --others --exclude-standard`, medida nos quatro censos), e o comando de teste do
  backend recorreu pela segunda vez com dois alvos numa linha só, ou seja, a prosa continuou não
  segurando onde não há mecanismo.
- **Medir enquanto a coisa medida se move (6 eventos: 2026-07-18, 07-19 ×2, 07-25 ×3).** Processo
  velho ainda servindo, arquivo pela metade, banco `ebgeo_test` compartilhado entre duas execuções,
  produção mutada por outro agente para o controle negativo dele, cobertura medida com seis agentes
  escrevendo. Abordagem atual: árvore parada antes de medir; banco por EXECUÇÃO e não por agente;
  mutação de produção para controle negativo só em cópia ou em janela declarada; afirmação de um
  agente sobre arquivo fora da sua lista é hipótese, não evidência.
- **O verificador quebra calado (8 eventos: 2026-07-18, 07-19, 07-24, 07-25 ×4, 08-14).** Hooks que
  liam variável inexistente, âncora de script de manutenção que nem sempre existe, `git rev-parse`
  ecoando no stdout ao falhar, reversão por regex que não reverteu, `git checkout` apagando a edição
  de outro autor, substituição multilinha que não casou em arquivo CRLF, e a lista de alvos do
  guarda de doc montada com `filter` em vez de asserção. Abordagem atual: toda operação de mutação e
  de reversão precisa DIZER se aconteceu (contar ocorrências antes e depois), e a conferência final
  vem por caminho independente do que produziu o resultado.
- **Cobertura vazia: verde que não prova nada (7 eventos, de 2026-07-18 a 07-26).** Regra que não
  casa com nada, teste cujo sítio nunca é alcançado, guarda com `skip` em ambiente de teste,
  igualdade entre dois `undefined`, sinal que não distingue o caminho certo do caminho nenhum.
  Abordagem atual: a pergunta "o que este verde estaria provando se o código estivesse errado?" a
  cada teste, controle negativo obrigatório, e três regras de lint próprias no backend
  (`backend/eslint-rules/`) que pegam a forma estrutural.
- **Comportamento observado registrado como esperado (6 eventos: 2026-07-19 ×3, 07-24, 07-25, 08-17).**
  Teste de caracterização cujo nome ou comentário descreve o defeito com precisão e o AFIRMA, o que
  transforma verde em cimento. Abordagem atual: para risco aceito de propósito, marcador `KNOWN GAP`
  que QUEBRA quando alguém fechar o buraco, em vez de asserção que o congela. O evento de 08-17
  mostra o limite dessa abordagem: o `KNOWN GAP` do auto-cadastro estava correto e mesmo assim
  virou cimento, porque ele afirmava o MECANISMO (a declaração é aceita) junto com a CONSEQUÊNCIA
  (e ela compra os projetos não publicados da OM), e só a segunda metade tinha de quebrar quando o
  buraco fechasse. Marcador de risco aceito assere a consequência, nunca as duas coisas na mesma
  asserção.

A lista de eventos abaixo é append-only e **não está em ordem estritamente cronológica**: agentes em
paralelo acrescentaram no fim ao mesmo tempo. Conte recorrência por este índice, não pela vizinhança
das linhas.

## Eventos

<!-- Formato: - AAAA-MM-DD `classe` [fonte] sintoma -> causa -> onde foi codificada -->

- 2026-07-18 `doc-sobre-codigo` [realidade] O `99-pendencias-e-desvios.md`, que se anunciava como referência única para integradores, documentava a permissão por atlas como `owner/write/read`. O CHECK real tem cinco níveis -> a mesma lacuna produziu o bug do `handleSelection`, que descartava a seleção do co-Gestor -> teste `docs-integridade.test.js` + regra dos 5 níveis no CLAUDE.md dos dois pacotes.
- 2026-07-18 `verificacao-fantasma` [sessao] Medi o backend três vezes achando que media o código novo. O `pkill` do Git Bash não mata processo Node do Windows, e o processo velho seguia servindo -> assumi que o comando teve efeito em vez de conferir -> matar por PID via `Get-NetTCPConnection` + `Stop-Process`; conferir a porta antes de medir.
- 2026-07-18 `regressao-propria` [realidade] O advisory lock que adicionei no push de sync era tomado depois de abrir a transação, retendo conexão do pool sem timeout; com o dispatcher WS sem `await`, um cliente sozinho esgotava o pool -> revisão multi-agente pegou; a maioria dos achados confirmados eram regressões minhas da mesma sessão -> `lock_timeout` + serialização por socket + `ServiceUnavailableError`.
- 2026-07-18 `teste-que-nao-prende` [realidade] Meu teste do P1 usava `/auth/me`, rota que relê o usuário por conta própria e retornaria 401 mesmo sem o fix do middleware -> o controle negativo revelou; sem ele o teste teria entrado verde e inútil -> troquei para `/atlas`; controle negativo virou passo obrigatório.
- 2026-07-18 `regressao-propria` [sessao] Fatiei a semeadura da wiki por tema e seis fatias escreveram o mesmo conceito com slugs diferentes; a dedupe por slug não via duplicata semântica -> eu tinha escrito o aviso "procure parecida e funda antes de criar" no `wiki-schema.md` horas antes de cair nele -> detector de duplicata por Jaccard >= 0.45 no `lint_wiki.py` (prosa não pegou; o detector pega).
- 2026-07-18 `doc-sobre-codigo` [realidade] A primeira auditoria da wiki acusou 125 erros, mas 117 eram contradições contra guias que eu mesmo tinha acabado de apagar -> marquei contradição contra a prosa em vez de contra o código, e o alvo deixou de existir -> viraram nota histórica; só contradição sobre código vivo é erro que acorda o gate.
- 2026-07-18 `verificacao-fantasma` [realidade] Commitei o teste de integridade rodando `npm run lint` na MESMA linha de comando do commit: a saída do lint apareceu depois do commit já ter passado, com 2 erros reais -> verificação que chega depois da ação não é verificação -> lint e teste em comando separado, antes do `git commit`.
- 2026-07-18 `teste-que-nao-prende` [realidade] A reescrita da wiki encurtou 1.054 citações para o basename (`sync.service.js:755`). O teste de integridade só casa caminhos com prefixo conhecido, então passava **vazio** nessas páginas: verde sem verificar nada -> reancorei no caminho real; o aviso "não cita nenhum arquivo de código" do `lint_wiki.py` é o detector dessa cobertura vazia (34 -> 14).
- 2026-07-18 `verificacao-fantasma` [realidade] Ao apagar o docs/deploy.md (removido) conferi as referências com `grep` em `CLAUDE.md` e `README.md` da raiz e dei por completo; sobraram 4 links em `backend/CLAUDE.md` e `backend/README.md` -> conferi um subconjunto e tratei como o conjunto -> o `docs-integridade.test.js` pegou as 8; a lição é não conferir referência à mão quando existe teste que varre tudo.
- 2026-07-18 `verificacao-fantasma` [realidade] Os DOIS hooks do `.claude/settings.json` estavam mortos desde sempre: liam `$TOOL_INPUT_FILE_PATH`, variável que o Claude Code não define (a entrada vem por JSON no stdin), e o de bloqueio ainda usava `exit 1`, que não bloqueia (precisa de 2 ou de `permissionDecision: deny`). Dois defeitos independentes, cada um bastando sozinho. A constituição prometia proteção de `deploy/` e lint a cada escrita; nenhum dos dois acontecia -> nunca provei o guard, só li a config -> reescritos em `.claude/hooks/*.js` e **provados por sonda** (escrever em `deploy/` e ver recusar); regra "se mexer no hook, sonde" em `.claude/rules/testing.md`.
- 2026-07-18 `regressao-propria` [realidade] Minutos depois de ler o achado de que `prepare-deploy.js` estava morto por usar `require` sob `"type": "module"`, escrevi `require` no meu próprio hook e o matei do mesmo jeito -> ler o defeito não imuniza contra cometê-lo; só a execução prende -> a sonda pegou na hora, e o hook agora distingue "lint limpo" de "lint não rodou", que era o silêncio que escondia o defeito.
- 2026-07-18 `teste-que-nao-prende` [realidade] O teste de conflito CRDT esperava até "A e B mostrarem a mesma cor" e só então cobrava que o Postgres batesse. Concordância entre clientes **não é** convergência: existe janela em que a op de A já chegou aos dois enquanto a de B ainda está em voo, e o poll saía nessa parada de caminho -> o predicado aceitava estado transitório como estado final, então o teste não conseguia distinguir "lento" de "divergência permanente", que seria bug de produto -> inverti a ordem (servidor decide o vencedor, clientes respondem a ele) e ancorei em `push.ack`; 15/15 com `--repeat-each=3`, e a mesma correção valeu para o teste de geometria, que tinha o predicado idêntico.
- 2026-07-18 `regressao-propria` [realidade] O teste do F5 que escrevi para provar durabilidade lia o vencedor do Postgres UMA vez e depois cobrava os clientes contra esse retrato; uma op pendente descarregava na reconexão e movia o vencedor **depois** da leitura, então o teste reprovava uma convergência correta -> eu tinha acabado de escrever que "concordância entre clientes não é convergência" e caí na versão vizinha, comparar contra estado já vencido -> helper `convergedValue` amostra servidor e clientes JUNTOS; e a lição de método é que `--repeat-each` isolado (15/15) NÃO substitui a suíte cheia para teste de concorrência, foi ela que reprovou meu trabalho duas vezes.
- 2026-07-18 `teste-que-nao-prende` [realidade] A regex do `docs-integridade` exigia a crase logo após a extensão, então **toda** citação no formato `arquivo:linha` escapava da checagem: 1116 não verificadas contra 210 verificadas, e `arquivo:linha` é justamente o formato que o `wiki-schema.md` manda usar. O teste passava verde medindo a minoria, e eu só descobri porque a reescrita do movimento para `frontend/` usou a mesma regex e deixou 1116 citações para trás -> cobertura vazia mede o que a regra casa, não o que a regra deveria casar -> sufixo `:linha` opcional na regex; as 1116 entraram na checagem e passaram.
- 2026-07-18 `regressao-propria` [realidade] Ao mover o pacote web para `frontend/`, o `RAIZ` do `docs-integridade` (calculado como `../..` a partir do teste) passou a apontar para `frontend/` em vez da raiz do monorepo, e a lista de documentos vigiados **zerou** -> o primeiro teste a falhar foi o guarda "lista os documentos vigiados (contra a lista esvaziar em silêncio)", escrito exatamente para esse caso -> guarda de lista não-vazia é barato e pega a classe inteira de "o coletor parou de coletar"; replicar onde houver varredura.
- 2026-07-18 `doc-sobre-codigo` [realidade] O `.claude/rules/architecture.md` listava quatro papéis de sessão (`owner/admin/editor/viewer`) onde o código tem seis, omitindo `manager` e `commenter` -> é exatamente a lista fechada que a constituição proíbe em dois lugares por já ter causado bug real duas vezes, cometida pelo arquivo que deveria codificar a lição -> corrigido com citação de `session-context.js:29-36`; a árvore de 92 linhas que apodreceu junto (faltavam `admin/` e `session/` inteiros) foi substituída pelo que um `ls` não conta.
- 2026-07-19 `teste-que-nao-prende` [realidade] Slide criado pela UI nunca chegava ao Postgres: o backend lia `data.briefing_id`, campo que o cliente não emite (o parentesco viaja no slot `mapId` do envelope, e o payload é camelCase). Zero linhas inseridas, ack `success: true`. As 20+ suítes de slide passavam porque **todas** escrevem a op no dialeto do servidor (`target: 'slide'`, snake_case, `briefing_id` explícito): provavam que o servidor funciona para um payload que o cliente nunca produz -> teste que fala a língua do servidor de volta para o servidor não prova integração, prova eco; é a mesma cegueira que escondeu a PK de `catalog_layers` (toda a suíte usava `randomUUID()` onde o cliente manda `'hillshade'`) -> `sync-slide-client-payload.repro.test.js`, que constrói a op copiando `createEmptySlide()` campo a campo, e a normalização entrou em `normalizeOperation` (junto de `deriveFeatureColumns`), não no ramo de create, para o UPDATE herdar a tolerância.
- 2026-07-19 `verificacao-fantasma` [realidade] O e2e de briefing cobre a cadeia completa, mas afirma `expectFullSync({ entityType: 'briefing' })` e deixa as edições de slide "pegarem carona" na op do pai. A linha do briefing persiste de verdade, então o teste fica honestamente verde enquanto a tabela `slides` continua vazia -> verificar o pai e inferir o filho é "conferir um subconjunto e tratar como o conjunto", agora em forma de teste e não de `grep` -> registrado no cabeçalho do repro; a lição é que cadeia completa se afirma sobre a ENTIDADE que se quer provar, não sobre o agregado que a carrega.
- 2026-07-19 `regressao-propria` [realidade] Consertar a ida dos slides colocou peso num caminho que nunca havia rodado com dado: o snapshot aliasava só `temporalCursor` para camelCase, e `applyRemoteSnapshot` salva o briefing verbatim no IndexedDB, então todo slide voltaria sem mapa, modelo nem foto. O defeito da ida escondia o da volta -> ao destravar um fluxo morto, o próximo elo é suspeito por definição: ele nunca foi exercido -> aliases `mapId`/`modelId`/`photoId` no snapshot, com controle negativo confirmando que só o teste da volta cai quando removidos.
- 2026-07-19 `teste-que-nao-prende` [sessao] O teste de Range que escrevi para o crash passava COM o bug presente: um diretório reporta tamanho 0, então `parseRange` devolve 416 antes de abrir stream nenhum, e o sítio sob teste nunca rodava. Eu tinha acabado de escrever no cabeçalho do arquivo que verde vazio é o risco -> a pergunta "o que este verde estaria provando se o código estivesse errado?" precisa ser feita a cada teste, não uma vez por arquivo -> trocado por asserção explícita de 416, com o ramo de Range coberto no teste unitário, onde as opções vão direto ao helper.
- 2026-07-19 `regressao-propria` [realidade] Rodei a suíte cheia em background enquanto ainda editava `stream-file.js`; ela carregou o arquivo pela metade e acusou ~10 suítes quebradas (sv360, rate-limit, sharing). Quase tratei isso como regressão real do fix -> medir enquanto se edita mede um estado que não existe, exatamente a família da `verificacao-fantasma` de medir o processo velho -> reexecutado com a árvore parada: 1308/1310, e as falhas restantes eram só as duas esperadas.
- 2026-07-19 `teste-que-nao-prende` [realidade] Nenhum dos testes de rate limit variava o endereço do cliente: afirmavam case-folding do username e o balde `${ip}:` para requisição sem username, ambos corretos e verdes com o bug presente. Um limitador global passa em todo teste que use um cliente só -> a lacuna não era asserção errada, era dimensão não variada; a auditoria também errou aqui, ao acusar os testes de afirmarem o colapso -> teste novo com dois X-Forwarded-For distintos, mais um caso provando que forjar o header não cria balde novo (o risco de confiar em saltos demais).
- 2026-07-19 `contrato-implicito` [sessao] Meu claim atômico revogava o token ANTES de checar expiração, então recusar um token expirado passava a mutá-lo, quebrando `auth-gaps` auth-08, que afirma `revoked_at` NULL nesse caminho. A suíte cheia pegou -> corrigi movendo `expires_at > NOW()` para dentro do próprio UPDATE, o que preserva o contrato e mantém a atomicidade, em vez de afrouxar o teste que me acusou -> a regra é que teste que reprova um fix meu é evidência, não obstáculo; só relaxei o `auth-hardening` (que exigia alarme em replay imediato) porque a janela de graça foi decisão explícita do usuário, e ali mudei só o tempo, mantendo a asserção de segurança.
- 2026-07-19 `verificacao-fantasma` [sessao] Rodei um teste isolado enquanto a suíte cheia corria em background; as duas criam e dropam o MESMO `ebgeo_test`, e o resultado foram dois testes de WS cancelados que atribuí quase que a regressão do meu fix. Sozinhos, 107/107 -> é a terceira vez hoje que meço um estado que não existe (processo velho, arquivo pela metade, agora banco compartilhado): medição concorrente com a coisa medida não é medição -> regra prática: enquanto a suíte cheia roda, não se roda teste nenhum; a espera é mais barata que o diagnóstico falso.
- 2026-07-19 `doc-sobre-codigo` [realidade] O `auth.js` isentava o principal `public-<uuid>` da reconciliação com um comentário dizendo que sua autoridade era "enforced by requireAtlasPermission". Era exatamente a guarda que NÃO existia no caminho HTTP: o gateway WS comparava a claim `atlasId`, o HTTP não, e um token de visitante de um atlas lia qualquer outro atlas público -> comentário que afirma uma garantia é tão perigoso quanto doc desatualizada, porque quem lê para de procurar -> guarda implementada nos dois mapeadores + comentário reescrito nomeando a data e mandando reconferir quem mexer no ramo.
- 2026-07-19 `teste-que-nao-prende` [realidade] O teste `maps-02b` se chamava "a public token reads OTHER public atlases (current cross-atlas behavior)" e afirmava 200, com comentário descrevendo o mecanismo do furo com precisão. A lacuna foi vista, descrita e então CONGELADA numa asserção em vez de reportada -> teste de caracterização sobre comportamento de segurança vira cimento: o próximo a mexer lê o verde como intenção -> invertido para 403, mantendo um caso que prova que o token ainda lê o próprio atlas (senão um fix que recusasse todo token público também ficaria verde).
- 2026-07-19 `cobertura-vazia` [realidade] `org_role` aparece 25 vezes nos dois pacotes e TODAS são leitura: nenhum INSERT, nenhum UPDATE, nenhuma tela. Todo usuário ficava no default 'viewer' para sempre e o gate de escrita por organização do sv360 era inalcançável — não falhava com erro, ficava permanentemente fechado -> coluna sem escritor é feature morta que nenhum teste denuncia, porque o caminho fechado devolve 403 que parece intencional -> tornada editável pelo admin, com um teste que promove, faz LOGIN e decodifica o JWT, porque os gates leem a claim e não o banco: persistir a coluna sem a claim chegar ao token pareceria corrigido e não mudaria nada.
- 2026-07-19 `regressao-propria` [sessao] Usei `t.none` para inserir a op marcadora, mas `INSERT_OPERATION` tem `RETURNING *` e o pg-promise rejeita `none` assim que volta linha: 500 em todo merge. O teste pegou na primeira execução -> `none/one/oneOrNone` do pg-promise são asserções sobre a CARDINALIDADE do retorno, não sinônimos de "não me importo com o resultado"; reusar uma query alheia exige olhar o RETURNING dela -> trocado para `oneOrNone` (o ON CONFLICT DO NOTHING pode devolver zero), com o porquê no comentário.
- 2026-07-19 `cobertura-vazia` [realidade] O CHECK de `audit_trail.action` reservava `SHARING_CHANGE`, `PERMISSION_GRANT` e `PERMISSION_REVOKE` desde a migração 001, e NENHUMA das cinco mutações de sharing emitia qualquer uma delas. Um admin filtrando por essas ações recebia zero linhas sempre, para todo atlas -> filtro que por construção nunca casa se lê como "nada aconteceu", não como "nunca foi ligado": é a forma mais silenciosa de lacuna, porque o schema já declarava a intenção e nada jamais falhou para apontar a ausência -> as cinco auditam na mesma transação da mudança, com `previous_permission` por auto-join (mudança de nível só é auditável se disser de onde veio).
- 2026-07-19 `teste-que-nao-prende` [realidade] Quarta ocorrência do MESMO padrão numa só rodada: ao corrigir um defeito, o teste que reprovava era um teste de caracterização cujo nome ou comentário descrevia o bug com precisão e o afirmava como esperado (maps-02b "current cross-atlas behavior"; users-coverage "keeps the atlas pointed at that (now inactive) id"; sync-gaps "stays tombstoned"; sync-authz-lock 403 no lote inteiro) -> não são quatro coincidências, é um hábito da suíte: registrar comportamento OBSERVADO como comportamento ESPERADO, o que faz o defeito parecer coberto e transforma verde em cimento -> a auditoria já previa isso (ponto cego estrutural 2) e acertou; a prática adotada para risco aceito de propósito é a inversa e funciona: marcador `KNOWN GAP` que QUEBRA quando alguém fechar o buraco.
- 2026-07-19 `teste-que-nao-prende` [sessao] Escrevi o teste do mailer mutando `config.mail` com defineProperty e os 12 casos falharam, inclusive dois que deveriam passar com o código atual: `config` é `Object.freeze` -> teste que não consegue alcançar o código é sinal de design, não obstáculo a contornar; refiz o FIX para expor função pura (`resolveVerificationBase`) e flag injetável (`exposeLink`) -> os dois ramos ficaram testáveis sem mutar global de módulo, e o fix ficou melhor por causa do teste, não apesar dele.
- 2026-07-19 `teste-que-nao-prende` [sessao] Meu teste do serializer chamava a função DIRETO, então remover `serializers: { err: ... }` da config do pino deixava os 10 casos verdes. Só descobri porque rodei o controle negativo -> função correta ligada a nada se lê exatamente como função funcionando: testar o componente não é testar a fiação -> caso que afirma `logger[pino.symbols.serializersSym].err === errSerializer`; e registrei explicitamente o que NÃO dá para afirmar (o `redact` aplica na escrita e o logger é `silent` em teste), porque asserir contra interno instável seria pior que admitir a lacuna.
- 2026-07-19 `cobertura-vazia` [sessao] Escrevi quatro casos provando que um `username` malformado não vira 500, e os quatro passavam COM o defeito presente: o `skip` do rate limiter desliga o middleware inteiro em teste quando `RATE_LIMIT_FORCE` não está setado, então o `keyGenerator` sob teste nunca era chamado. Só o controle negativo denunciou -> escrevi a documentação desse padrão nesta mesma sessão e o cometi logo depois; ler o defeito não imuniza, e a única barreira que pegou foi executar o controle negativo -> flag ligada no bloco, e o controle negativo passou a derrubar os quatro.
- 2026-07-19 `regressao-propria` [sessao] Adicionei um comentário SQL com crase dentro de uma query que é template literal de JS: a crase fechou a string e o arquivo virou SyntaxError. O runner reportou só "test failed", sem linha -> erro de sintaxe em módulo importado aparece como falha genérica do arquivo de teste, longe da causa; rodar `node --test` direto deu a mensagem real em um passo -> comentário sem crase, com nota explicando o porquê para o próximo.
- 2026-07-19 `regressao-propria` [sessao] Ao acender a persistência de slides eu criei um defeito novo: `GET_BRIEFING_SLIDES` não tem `ORDER BY`, e o editor e o apresentador indexam `briefing.slides[i]` direto. Enquanto a tabela estava sempre vazia isso não importava; assim que passou a ter dado, a apresentação passa a tocar fora de ordem -> TERCEIRA vez na sessão que destravar um fluxo dormente quebra o elo seguinte (slides, mailer, agora ordem); a regra já está escrita e eu continuo descobrindo instâncias, então o certo é procurá-las ATIVAMENTE em vez de esperar o sintoma -> array ordenado por `slide_order` no snapshot, com teste que empurra os slides fora de sequência de propósito.
- 2026-07-19 `contrato-implicito` [realidade] O servidor manda `reason` junto do ack de op recusada e o cliente descartava: a op era removida da fila (correto, replay de negação nunca sucede) mas o usuário não sabia de nada — a entidade já sumiu localmente, o servidor a manteve, e o próximo snapshot a traz de volta sem explicação, minutos depois -> descarte silencioso de erro é pior que fila travada, porque troca um sintoma diagnosticável por um fantasma; o servidor já mandava a informação e faltava um `showWarning` -> aviso por razão distinta (não por op, senão N toasts iguais) + 3 casos no `sync-engine.test.js`.
- 2026-07-19 `regressao-propria` [realidade] Meu `normalizeSlidePayload` mapeava `mapId` do cliente para `slides.map_id`, que é `UUID REFERENCES maps(id)` — mas o `slide.mapId` do frontend guarda o NOME de exibição do mapa (`getCurrentMapNameSync()`, `mapNames[0]`, `<option value=nome>`). Nome em coluna UUID levanta 22P02, que aborta a transação do LOTE INTEIRO; como o cliente só re-enfileira em não-2xx, ele replica o lote envenenado para sempre: o sync daquele usuário para permanentemente e em silêncio. E o editor seta o campo sozinho, com autosave, então nem precisa de ação deliberada -> eu corrigi um contrato lendo os dois lados e ainda assim assumi que campos de mesmo NOME tinham o mesmo SENTIDO; `mapId` do cliente e `map_id` do servidor são homônimos de semântica diferente -> guarda de UUID igual à que o próprio arquivo já aplica em authorId e layer_id, com teste de lote misto provando que a op irmã sobrevive.
- 2026-07-19 `verificacao-fantasma` [sessao] Essa regressão passou pela minha suíte inteira (1425 verdes) porque TODOS os meus testes de slide mandavam `mapId: map.id`, um UUID de verdade. Eu escrevi o payload "do cliente real" campo a campo copiando `createEmptySlide()`, mas preenchi o VALOR com o que o servidor esperava, não com o que o cliente manda -> copiar a FORMA do payload do cliente não basta; o valor também é contrato, e foi exatamente aí que a auditoria cruzada pegou o que 11 testes meus não pegaram -> auditoria dos dois pacotes virou passo obrigatório para toda mudança de normalização de payload.
- 2026-07-19 `contrato-implicito` [realidade] `slide.mapId` do cliente e `slides.map_id` do servidor são HOMÔNIMOS de sentido oposto: o primeiro é nome de exibição, o segundo é FK UUID. A associação slide↔mapa nunca funcionou em nenhum dos dois sentidos, e ninguém percebeu porque a tabela de slides estava sempre vazia — o defeito maior escondia o menor -> traduzir nos dois sentidos no servidor, que é o único lado que tem os dois: nome→UUID na escrita (escopado ao atlas, desempate determinístico pelo mais antigo, porque nome de mapa não é único) e UUID→nome no snapshot -> teste de ROUND-TRIP, porque cada perna isolada pode parecer certa com o par inútil.
- 2026-07-19 `doc-sobre-codigo` [sessao] Anotei 36 correções em `bugs-backend.md` ancorando a inserção em `\n---`, mas nem todo bloco de achado termina com esse separador: TODAS as notas caíram em 4 achados errados. O relatório passou a afirmar que outros bugs estavam corrigidos, e os 35 que eu realmente corrigi apareciam como pendentes -> doc errada é pior que ausente, e aqui ela foi gerada pela minha própria ferramenta, sem nada checando; a âncora certa era o próximo cabeçalho `### N.`, que sempre existe -> reparo com re-inserção por cabeçalho + validação obrigatória antes de gravar.
- 2026-07-19 `cobertura-vazia` [sessao] O primeiro reparo passou verde e ainda estava errado: eu validava a CONTAGEM de notas por achado, que continuava batendo enquanto o mapeamento inteiro estava deslocado em um. Contagem certa, conteúdo trocado -> validar cardinalidade não valida identidade; é a mesma família de "o que este verde estaria provando" aplicada a script de manutenção, não a teste -> mapeamento por FRASE DISTINTIVA de cada nota e validação que exige a frase DENTRO do bloco do achado, mais conferência por caminho independente (awk) para não confiar no mesmo script que produziu o resultado.
- 2026-07-25 `verificacao-fantasma` [realidade] `npm run lint` e `npm test` na RAIZ delegavam só para `frontend/`, e são exatamente os dois comandos que a constituição e o `.claude/rules/testing.md` prescreviam como *a* verificação de lógica antes de qualquer commit. Uma mudança só de backend, verificada exatamente como mandado, rodava zero teste e zero lint de backend e voltava verde -> é a forma "conferir um subconjunto e tratar como o conjunto" dentro do próprio documento que a proíbe, e o alvo era o guarda, não o código (os dois pacotes estavam limpos) -> `lint`/`test` da raiz passaram a rodar os dois pacotes, o lint do backend ganhou `--max-warnings 0` (que o frontend já tinha: o mesmo warning reprovava um pacote e passava no outro), e as duas linhas de doc foram corrigidas. Controle negativo com erro de sintaxe plantado no backend: o comando antigo passa verde, o novo reprova.
- 2026-07-25 `verificacao-fantasma` [sessao] Três controles negativos meus, em achados diferentes, usaram regex para reverter o fix e NÃO reverteram nada — os testes passaram verdes e eu quase os aceitei como prova. Nas três vezes o que me salvou foi o próprio script imprimir o resultado da remoção (`guarda removida: false`, `antes: 6 depois: 6`) -> reverter por regex é frágil de um jeito silencioso, e o silêncio cai exatamente do lado que confirma o que eu queria ver -> passei a reverter com `git checkout HEAD -- <arquivo>`, que é exato; e o padrão que vale é o do `--max-warnings`: a operação de reversão precisa DIZER se aconteceu, senão o controle negativo vira teatro.
- 2026-07-24 `teste-que-nao-prende` [realidade] Ao corrigir o `canToggleLock` (que negava o cadeado ao editor logado no próprio mapa LOCAL), **cinco** testes reprovaram o fix: dois afirmavam `false` para editor/viewer online e três dependiam disso no `toggleMapLock`. Todos montavam a cena com `setOnline()`, que mexe só na sessão e deixa `isRemoteStoreSync()` em `false` — ou seja, afirmavam o bloqueio sobre o store local, exatamente o caso que o princípio P1 proíbe bloquear -> é a quinta ocorrência do mesmo hábito da suíte (registrar comportamento OBSERVADO como ESPERADO), agora do lado do frontend; o teste não estava errado por acidente, estava desenhado a partir do comportamento -> bloco reescrito separando store local de atlas remoto, com controle negativo: 28/28 com o fix, 3 falham sem ele.
- 2026-07-24 `verificacao-fantasma` [sessao] Afirmei ao usuário que o `isUUID` do 360 era "código morto, sem nenhum consumidor". Meu grep tinha `grep -v streetview-api.service.js`, que excluía justamente o arquivo onde estão as duas chamadas -> filtrei o alvo para fora da busca e li o vazio como ausência; a wiki, que eu estava auditando, já descrevia os dois call sites corretamente -> era defeito vivo: `getPhotoDisplayName` devolvia o UUID cru como nome exibido no editor de briefing e na aba de feições 360. Regex corrigida para casar a forma em qualquer versão, com controle negativo (7/7 com o fix, 3 falham com a regex v4).
- 2026-07-24 `verificacao-fantasma` [sessao] Minha tabela comparando os 17 commits do `main` com o dev classificou como "DIFERE" arquivos que estavam **AUSENTES**, incluindo o módulo KMZ inteiro. `git rev-parse branch:caminho-inexistente` escreve a mensagem no stderr **e ecoa a string no stdout**, então `$(cmd 2>/dev/null || echo AUSENTE)` capturava as duas coisas e nunca batia com o blob do outro lado -> o verificador falhou em silêncio e produziu uma categoria errada, não um erro; eu ia planejar o porte sobre essa tabela -> pego porque um `git ls-files` independente contradisse a tabela; refeito com `git cat-file -e`. É a mesma lição de 2026-07-19 (validar por caminho independente do que produziu o resultado), agora na análise e não no script de manutenção.
- 2026-07-24 `premissa-inventada` [sessao] Recomendei ao usuário que bastava `npm update dompurify` porque "o `^3.3.1` já permite a versão corrigida, só o lockfile prende" — afirmação verdadeira no `main` e falsa no monorepo, onde `frontend/package-lock.json` estava no `.gitignore`. O `npm update` rodou, subiu o `node_modules` e o `git status` voltou VAZIO -> assumi a topologia de um pacote lendo o outro, dentro da mesma sessão, sem conferir onde o lockfile era versionado -> a correção real era subir o piso no manifesto; os lockfiles passaram a ser versionados nos dois pacotes.
- 2026-07-19 `verificacao-fantasma` [sessao] Declarei que os 195 resultados da auditoria do frontend "não sobreviveram" depois de um `find` que voltou vazio. Procurei só em `AppData/Local/Temp/claude`, que é scratchpad e saída de tarefa; artefato de workflow mora em `~/.claude/projects/<projeto>/<sessão>/`. O usuário mandou procurar de novo e estavam lá, íntegros: 230 transcrições e 195 resultados no `journal.jsonl` -> busca que volta vazia prova que não achei, não que não existe; a diferença importa mais quando a conclusão é "esse trabalho se perdeu", porque ela autoriza refazer tudo -> caminho exato das duas raízes gravado na memória `auditoria-tres-eixos`, com a instrução de procurar nas DUAS antes de declarar perda.
- 2026-07-25 `doc-sobre-codigo` [realidade] `.claude/rules/architecture.md` afirmava que o boot restaura "session + last remote atlas" e citava um `reconnectLastAtlas` com ZERO ocorrências em `frontend/src/`; o que roda é `openAtlasChooserOnBoot`, que DESCARTA dado remoto órfão e abre o Atlas Drive. A wiki tinha a contradição registrada e pendente havia meses, e nada quebrava, porque o `docs-integridade.test.js` valida o CAMINHO citado e nunca o SÍMBOLO -> arquivo de regra é a doc mais cara de estar errada, porque é carregada como instrução em toda sessão de agente e quem planeja a partir dela propõe código de reconexão que duplica o Atlas Drive -> regra nova no `docs-integridade.test.js` que afirma que todo símbolo citado entre crases existe em algum código; na primeira execução achou este e o `needsMigration` de `.claude/rules/common-tasks.md` (a função real é `detectMigrationNeeded`), os dois em arquivo de regra.
- 2026-07-25 `verificacao-fantasma` [sessao] A regra de símbolo que eu acabara de escrever se auto-satisfazia: o índice de "existe no código" varria os comentários, então documentar no comentário do PRÓPRIO teste que `reconnectLastAtlas` não existe fazia o nome entrar no índice e o guarda passar a declarar que ele existe. Os dois únicos achados reais sumiram da lista no instante em que registrei a correção -> é a forma mais traiçoeira da classe: o verificador abençoando exatamente o que devia acusar, e o gatilho é o ato de documentar; qualquer checagem "X existe?" sobre um corpus que inclui prosa sobre X tem esse defeito -> comentário deixou de contar como existência (`semComentarios`), o que remove 21% dos tokens do índice (44285 para 34824), com três controles negativos: pendurado reprova, pendurado citado só em comentário CONTINUA reprovando, e definido como código de verdade passa.
- 2026-07-25 `verificacao-fantasma` [sessao] Dois agentes paralelos se contradisseram sobre `INSERT_IMAGE_WITH_ID`: um afirmou `ON CONFLICT (id) DO UPDATE` com sobrescrita cross-atlas e ESCREVEU isso na wiki; o outro afirmou que não há `ON CONFLICT` e usou justamente adicionar um para mutar o código no seu controle negativo. Lendo o arquivo, o segundo estava certo: o primeiro leu produção DENTRO da janela de mutação do segundo e tomou a mutação por código -> é a lição de "medição concorrente com a coisa medida não é medição" numa forma nova: com agentes paralelos na MESMA árvore, o controle negativo de um vira fonte falsa para outro, e o sintoma é doc afirmando um defeito que não existe -> regra para o orquestrador: agente que muta produção para controle negativo tem que fazê-lo em cópia ou em janela declarada, e afirmação de um agente sobre arquivo fora da sua lista é hipótese, não evidência; arbitrei lendo o arquivo e reescrevi o parágrafo errado.
- 2026-07-25 `verificacao-fantasma` [realidade] TERCEIRA ocorrencia da mesma classe em uma semana: script da raiz que delega para UM pacote e se anuncia como se cobrisse os dois. Depois de consertar `lint` e `test`, sobraram `test:coverage` (apontando so para o frontend, justamente quando o backend acabara de ganhar piso de cobertura, o que tornava o piso inalcancavel pelo comando da raiz) e `lint:fix` -> o livro-razao manda mudar a ABORDAGEM quando uma correcao recorre, e consertar o script pela terceira vez seria re-anotar: a abordagem nova e afirmar a PROPRIEDADE -> `frontend/tests/unit/scripts-da-raiz.test.js`, quatro casos: script que delega para um pacote so precisa dizer no nome (`:frontend`/`:backend`) ou entrar numa lista de excecoes COM MOTIVO; comando encadeado precisa alcancar os dois de fato (resolvendo a cadeia, senao `lint:frontend && lint:frontend` passaria); e todo script que existe num pacote precisa ser alcancavel pela raiz. Achou o `lint:fix` na primeira execucao. Controle negativo: devolver `test` ao estado antigo reprova dois casos.
- 2026-07-25 `doc-sobre-codigo` [sessao] Ao corrigir um caminho morto no `backend/CLAUDE.md`, escrevi o caminho morto ENTRE CRASES dentro do parenteses que explicava que ele nao existe, e o `docs-integridade` reprovou na hora -> e a mesma forma do defeito do guarda de simbolo desta manha: o ato de documentar que algo nao existe faz o verificador ve-lo como citacao viva; a diferenca e que aqui o guarda funcionou e me pegou em vez de me abencoar -> reescrito sem crase, e a substituicao usa wikilink de proposito, porque wikilink tem guarda que resolve o alvo e caminho nu em prosa nao tem nenhum.
- 2026-07-25 `verificacao-fantasma` [sessao] Minha propria regra de 2026-07-25 ("reverta o controle negativo com `git checkout HEAD -- <arquivo>`, que e exato") se voltou contra um agente: numa arvore com edicoes concorrentes, o checkout restaura o arquivo INTEIRO e apagou a edicao legitima que ele mesmo tinha feito em `config.js` minutos antes -> a regra estava certa contra o problema que eu tinha (regex que nao reverte nada) e errada contra o problema novo (varios autores no mesmo arquivo); precisao e exatidao nao sao a mesma coisa quando o alvo se move -> forma corrigida: copiar o arquivo para backup ANTES de mutar e restaurar do backup, que e exato E nao toca no que nao e da mutacao. `git checkout` continua valendo quando o arquivo tem um autor so.
- 2026-07-25 `verificacao-fantasma` [sessao] Um agente rodou substituicao multilinha por script sobre arquivo CRLF; o padrao nao casou, nada foi mutado, e o controle negativo voltou "21/21 verde" — lido como "o teste nao prende", quando o certo era "a mutacao nao aconteceu" -> e a mesma familia das tres reversoes por regex de ontem, agora na ida em vez da volta: operacao de mutacao que nao DIZ se aconteceu transforma controle negativo em teatro -> passou a contar as ocorrencias antes e depois de substituir, e a contagem virou parte do relatorio.
- 2026-07-25 `verificacao-fantasma` [sessao] Uma execucao reprovou ~80 testes de uma vez e o motivo nao era codigo: `TEST_DB_NAME` reutilizado enquanto outra execucao ainda segurava conexoes, dando `permission denied to terminate process` no drop e falha em massa no runner -> diagnosticar pelo `x` teria produzido conclusao falsa sobre 80 testes; o sinal estava na mensagem do runner, nao na contagem -> banco novo por EXECUCAO, nao so por agente, quando ha paralelismo.
- 2026-07-25 `teste-que-nao-prende` [realidade] O teste `atlas-transfer-ownership` se chamava "a stranger cannot transfer (and cannot learn the atlas exists)" e o comentario do proprio corpo dizia, com estas palavras, que "404 is reserved for an atlas that does not exist" -> nome e corpo se contradiziam desde que o arquivo existe, e ninguem notou porque os dois estavam certos sobre coisas diferentes: o nome descrevia a INTENCAO e o corpo o COMPORTAMENTO -> os dois agora concordam, e a licao generalizavel e que nome de teste tambem e afirmacao e merece ser lido contra o codigo, nao so contra o corpo do teste.
- 2026-07-25 `cobertura-vazia` [sessao] Ao provar que o 404 do estranho e indistinguivel do 404 de atlas inexistente, comparar os dois envelopes passaria verde mesmo se ambos viessem sem campo `error`, porque `undefined === undefined` -> assercao de IGUALDADE entre duas respostas e vacua por construcao quando o campo comparado pode faltar nas duas -> cada comparacao ganhou ancora anti-vacuidade (`assert.equal(body.error.code, 'NOT_FOUND')`) antes do par.
- 2026-07-25 `verificacao-fantasma` [sessao] Ao fechar o eixo de testes, quatro execucoes da suite cheia reprovaram testes DIFERENTES a cada vez (boot+migracoes, depois sync, depois cross-tenant), e cada um passava isolado e passava tambem na execucao so de `tests/integration` (1900/1900). Dois eram defeito real de contrato meu (403 virando 404) e foram corrigidos; dois eram acoplamento -> a licao e sobre o DIAGNOSTICO: com ~120 arquivos de teste novos numa base compartilhada, "passou isolado" nao prova ausencia de defeito e "reprovou junto" nao prova regressao, e chamar de flake e a saida preguicosa que teria escondido os dois reais -> o protocolo que funcionou foi bissetar por conjunto (so integration, depois pares suspeitos) ate a mensagem real aparecer, e so entao decidir. Um dos acoplamentos ficou identificado e corrigido: `migrations-tracking-vs-disco` prendia `ranks === 19`, contagem absoluta de tabela que `ranks.test.js` escreve.
- 2026-07-25 `verificacao-fantasma` [sessao] `npm test -- <a> <b>` no backend roda SO o primeiro padrao: o `run-tests.js` faz `args.find(a => !a.startsWith('--'))`. Eu passei dois arquivos para reproduzir uma interferencia, o runner rodou um, deu 3/3 verde e eu quase conclui que a hipotese estava refutada -> comando que aceita N argumentos e usa 1 devolve verde sobre um subconjunto silencioso, que e a forma "conferir um subconjunto e tratar como o conjunto" na propria ferramenta -> use UM padrao com chaves (`"tests/integration/{a,b}.test.js"`), e confira a CONTAGEM de testes contra a esperada antes de acreditar no verde.
- 2026-07-25 `verificacao-fantasma` [realidade] O E2E full-chain (`npm run test:e2e`, vitest contra backend real, ~35s) estava com **7 falhas havia um dia**, desde `aec63f8`, e ninguem viu. Motivo estrutural: ele NAO faz parte do `npm test`, e a constituicao manda rodar `npm run lint` + `npm test`. E o mesmo defeito de `lint`/`test` cobrirem so o frontend, num nivel acima: agora nao e o script que cobre um subconjunto, e o CONJUNTO DE GUARDAS que cobre um subconjunto -> a constituicao chama esse E2E de "o guarda dessa fronteira" e ao mesmo tempo nao o inclui no comando que ela mesma prescreve; guarda fora do comando que se manda rodar e guarda desligado -> `test:e2e` entrou no encadeamento do `test` da raiz (custa 35s e ja exige o mesmo Postgres que o `test:backend`), e o motivo ficou registrado na lista de excecoes do `scripts-da-raiz.test.js`, onde a proxima pessoa a mexer vai ler.
- 2026-07-25 `verificacao-fantasma` [sessao] Ao ver as 7 falhas do E2E logo depois de quatro mudancas de contrato, minha primeira leitura foi "regressao de hoje". Testei antes de concluir: troquei `sync.service.js` pela versao de HEAD e a falha permaneceu, e o `git log` mostrou que os specs nao sao tocados desde a mudanca do monorepo, muito antes da mudanca de contrato que os invalidou -> proximidade temporal entre uma mudanca e uma falha e evidencia fraca quando o guarda nao roda ha dias: sem data de ultimo verde, "quebrou agora" e indistinguivel de "quebrou quando ninguem olhava" -> a bissecao por arquivo unico (copia, troca, mede, restaura) resolveu em dois passos.
- 2026-07-25 `teste-que-nao-prende` [sessao] Ao cortar sessao por `sessions_valid_from`, a escolha entre `iat < corte` e `iat <= corte` parecia detalhe de borda. Nao era: o `iat` do JWT tem resolucao de SEGUNDO, e com `<` a reproducao NATURAL do proprio achado (login, revogacao, replay) roda inteira dentro de um segundo de relogio e volta 200 -> seria um fix que o teste do proprio bug nao enxerga, e o agente descobriu isso por FALHA ao escrever o teste, nao por deducao; a assimetria que decidiu e que super-rejeicao e auto-curavel (401, refresh, novo login) e sub-rejeicao nao e -> `<=`, com caso unitario isolando `iat` IGUAL ao corte, que e o unico que separa as duas regras. Registro tambem que eu tinha dado ao agente uma premissa ERRADA ("a troca de senha emite par novo logo depois de revogar"): nada neste repositorio cunha token no mesmo fluxo que revoga, e ele refutou em vez de obedecer.
- 2026-07-25 `verificacao-fantasma` [sessao] Pedi a um agente que "provasse com o E2E" que o limitador novo nao estrangula o boot. Ele rodou, passou, e **disse que aquilo nao provava nada**: o limitador pula sob `NODE_ENV=test`, como os outros quatro do projeto -> eu prescrevi uma verificacao que nao verificava, e a instrucao vinha de quem passou a sessao inteira caçando exatamente isso; guarda com `skip` em teste torna verde de suite uma evidencia vazia sobre o proprio guarda -> ele substituiu por tres provas reais (limitador forcado ativo no e2e, medicao da suite de boots em navegador real com pico de 31 contra teto de 600, e caso dedicado do 429), e a licao para mim e conferir se o que mando medir esta LIGADO no ambiente onde mando medir.
- 2026-07-25 `verificacao-fantasma` [sessao] Um agente reportou que o piso de cobertura estaria reprovando (86,47% de linha contra piso de 96) e eu quase tratei como regressao. Medido com a arvore QUIETA depois que os seis agentes pousaram: 98,14%. A diferenca nao era o trabalho dele: com varios agentes escrevendo, suite inteira deixa de CARREGAR (arquivo meio escrito), e cada suite que nao carrega derruba a cobertura das linhas que ela exercitaria -> cobertura medida em arvore movel nao mede cobertura, mede quantas suites conseguiram subir naquele instante; e o numero mente para BAIXO, que e a direcao que parece alarme legitimo -> nenhuma decisao sobre piso, com paralelismo ativo, antes de medir com a arvore parada.
- 2026-07-25 `contrato-implicito` [sessao] Instrui um agente a consertar a corrida de transferencia de posse "comparando o `atlas.owner_id` ja lido no inicio da transacao com o `currentOwnerId`", e chamei isso de conserto de uma linha. Ele recusou com razao de MVCC: sob READ COMMITTED as duas transacoes leem o dono PRE-transferencia antes de qualquer escrita, entao as duas comparacoes passam e nenhuma vira 409 -> so a reavaliacao do `WHERE` depois do lock de linha decide, e isso exige `owner_id` estar DENTRO do `WHERE` mais checagem de `rowCount` -> a licao e sobre mim: "ler antes e comparar depois" e a forma classica do check-then-act, e eu a prescrevi como conserto de corrida; ele mediu (`repeatRace` deu 4 vencedores em 8 de 8 execucoes com a versao que eu sugeri) em vez de obedecer.
- 2026-07-25 `cobertura-vazia` [sessao] A primeira versao dos dois casos de determinismo do merge (achado 83) ficava VERDE com o `ORDER BY` removido. O agente sondou o plano: e index scan sobre `idx_cesium3d_map`, e `SET data = data` e HOT update, entao o indice segue apontando para a tupla original e a ordem fisica nunca vira num teste -> "mudar a linha para baguncar a ordem" e intuicao que o Postgres nao honra; ordem fisica nao e alcancavel por UPDATE trivial -> reescrito para separar ordem de armazenamento de ordem de `created_at` por CONSTRUCAO (insere a mais nova primeiro, depois retroage a outra), que discrimina.
- 2026-07-25 `premissa-inventada` [sessao] Para consertar o `413` que o `/images/bulk` devolve a token expirado, implementei no servidor a resposta obvia: `401` cedo, antes de escolher o parser de corpo. Os testes voltaram `ECONNRESET` nos tres casos de corpo grande -> responder e encerrar com megabytes ainda em voo faz o Node destruir o socket, e o cliente le erro de conexao em vez do status; ou seja, o `401` que eu tinha acabado de escrever era INDELIVERAVEL exatamente nos casos que motivaram o fix, e o caso de corpo pequeno (que responde em 2ms) teria me deixado concluir o contrario se eu so tivesse olhado para ele -> a alternativa de drenar o corpo antes de responder devolveria o status ao custo de deixar chamador anonimo empurrar os 50MB inteiros, que e a amplificacao que aquele guarda existe para impedir; revertido no servidor e resolvido no cliente (`_ensureFreshAccessToken`), com o porque e a alternativa recusada gravados em `docs/wiki/refresh-token-rotacao.md` para que o proximo nao refaca a tentativa. Licao generalizavel: em HTTP, "responder antes" nao e so uma questao de ordem de middleware, e uma questao de quem ainda esta escrevendo no socket.
- 2026-07-26 `verificacao-fantasma` [realidade] Consertei o link 360 compartilhado, rodei UMA vez no Playwright, deu verde e declarei resolvido. O usuário testou e continuava quebrado -> comportamento probabilístico medido uma vez não é medido: uma corrida com ~50% de falha passa em metade das verificações honestas, e o verde único é indistinguível do determinístico -> medi em série (3/8 antes do fix, 20/20 depois) e codifiquei a forma nova na seção Verificação do CLAUDE.md.
- 2026-07-26 `verificacao-fantasma` [sessao] Passei várias rodadas diagnosticando um `isStreetView360Open() === false` que convivia com `body.streetview-active === true`, tentando explicar como a função podia rodar sem setar o próprio estado. Não podia: meu `page.evaluate` fazia `import('/src/js/...')` e recebia OUTRA instância do módulo, porque o Vite serve o arquivo que acabei de editar com `?t=` de HMR -> instrumento que carrega uma segunda cópia do sujeito mede a cópia, não o sujeito -> passei a medir por rede e DOM (independentes de identidade de módulo) e registrei a armadilha no MEMORY.md.
- 2026-07-26 `verificacao-fantasma` [sessao] O usuário reportou `Photo not found: MULTICAPTURA_... (HTTP 502)` e eu atribuí ao backend reiniciando pelo `node --watch`, porque eu estava editando arquivos naquele instante. Havia mesmo um restart, e a atribuição parecia sólida — mas o erro real era outro (o visualizador navega por NOME e pedia `/photos/:uuid`), e só apareceu quando o mesmo erro voltou com 422 -> causa plausível e contemporânea não é causa verificada; o 502 explicava o código do erro e eu nunca conferi se o pedido em si fazia sentido -> corrigi em voz alta com o usuário e prendi por teste de roteamento por forma do identificador.
- 2026-07-26 `cobertura-vazia` [sessao] Para provar que a seta de borda gira em vez de navegar, medi "nenhuma requisição de metadado após o clique" e deu verde. Clicar no vazio produz exatamente o mesmo verde -> o sinal não distinguia o caminho certo do caminho nenhum, e eu quase o reportei como prova -> troquei por teste unitário da DECISÃO (fora de vista gira, na tela navega), com controle negativo que derruba 2 dos 8 casos.
- 2026-08-13 `verificacao-fantasma` [realidade] Um teste de integração falhava com `ECONNRESET` umas 3 vezes em 8 e vinha sendo lido como flake de ambiente. Não era: o gate de permissão do upload de imagem responde antes do multer, e o node derrubava o socket com o corpo ainda subindo, então o cliente recebia erro de conexão no lugar do 403 -> o sintoma depende do TAMANHO do corpo (o que cabe no buffer do socket já chegou quando a resposta sai), e a suíte sobe 70 bytes, que é justamente a faixa onde o defeito é intermitente; "falha às vezes" foi tratado como propriedade do ambiente quando era propriedade do payload -> tornei a interleaving perdedora determinística em vez de medir estatística: 70 bytes 0/20, 3 MB 20/20 antes; 0/20 nos dois depois. O teste de regressão usa o corpo grande de propósito, e o teste original saiu de 3/8 e 5/8 para 0/20.
- 2026-08-13 `premissa-inventada` [sessao] Consertei o ECONNRESET acima drenando o corpo antes de responder, e a decisão de 2026-07-25 (quatro linhas acima neste arquivo) tinha considerado e RECUSADO exatamente essa solução, porque ela deixa um anônimo empurrar 50 MB. Eu li o livro-razão só depois, para escrever a linha do conserto -> o registro cumpriu o papel, mas tarde: uma alternativa já rejeitada não fica marcada no CÓDIGO que ela protege, então quem chega pelo arquivo (e não pelo livro) a reinventa; foi o que fiz -> a reconciliação usa o critério que o `app.js` já aplicava ao parser ampliado do bulk (principal verificado mais teto), e agora o PORQUÊ está no comentário do `drain-on-error.js`, ao lado da linha `if (!req.user)`, com um teste que falha se ela sumir. Regra que tiro para mim: antes de implementar defesa em caminho quente, procurar no livro-razão pela classe do problema, não só depois, para escrever a nota.
- 2026-08-14 `verificacao-fantasma` [sessao] Conduzi uma auditoria de cobertura TOTAL do frontend, gerei um COBERTURA.md com 907 arquivos e declarei "zero pendentes" em três relatórios. O inventário tinha sido montado a partir de uma lista de diretórios escrita à mão (`src/js`, `src/css`, `tests/unit`, `tests/integration`, `tests/e2e-ui`, `tests/helpers`) e omitia `tests/e2e/` (55 arquivos, o guarda da fronteira entre os dois pacotes) e `tests/store/` (19) -> é a forma mais cara de "conferir um subconjunto e tratar como o conjunto", porque o artefato produzido AFIRMA a completude: quem ler o COBERTURA.md não tem como saber que o universo estava errado, e a auditoria inteira passou a mentir sobre si mesma. Pego por acaso, ao conferir um número do relatório contra `git ls-files tests` -> a lição não é "revisar a lista": é que **inventário se deriva do versionamento, nunca se enumera**. A lista de diretórios devia ter sido `git ls-files frontend/` menos as exclusões declaradas, que é a única forma cujo erro aparece como exclusão explícita em vez de ausência silenciosa. Corrigido lendo os 77 arquivos omitidos e refazendo os números; a coluna de exclusões do COBERTURA.md agora lista o que saiu E por quê (os cinco relatorios da auditoria, esse inventario incluso, foram absorvidos e removidos da raiz em 2026-08-14).
- 2026-08-14 `premissa-inventada` [sessao] Um agente reportou que `tests/e2e/` "volta verde tendo executado zero teste de contrato" quando não há Postgres, porque o `global-setup` seta `EBGEO_E2E_SKIP` e os 54 specs fazem `describe.skipIf`. A descrição do mecanismo estava CERTA e a conclusão estava ERRADA: existe desde sempre um `_backend-required.e2e.test.js`, deliberadamente NÃO gateado, que falha exatamente nesse caso, com opt-out explícito por `EBGEO_E2E_ALLOW_SKIP`. Eu aceitei o achado, inverti o padrão do `global-setup` para falhar, criei uma segunda variável de opt-out com outro nome e só descobri o guarda existente quando ele apareceu no MEU controle negativo, como o único teste vermelho -> o agente enumerou os specs, viu 54 `skipIf` e concluiu ausência de guarda sem procurar por um guarda que, por definição, é o ÚNICO arquivo sem `skipIf`: a evidência da existência dele era justamente o que não casava com o padrão da busca -> revertido. A lição para mim é de sequência, não de conteúdo: antes de CRIAR um guarda contra uma classe de falha, procure o guarda que já existe contra ela, e procure pelo que ele faria de diferente, não pelo que os outros fazem igual. O controle negativo pegou, mas ele veio depois de eu já ter escrito a correção.
- 2026-08-14 `verificacao-fantasma` [sessao] Movi este arquivo e o `docs/MEMORY.md` da raiz para `docs/` a pedido do dono e rodei o guarda de doc antes de ajustar a lista `ALVOS`: passou 8/8. O guarda monta a lista com `ALVOS.filter((f) => existsSync(...))`, e filtro nao e verificacao -> os dois documentos sairam da vigilancia em SILENCIO, e o teto de tamanho do MEMORY.md, que abria com `if (!existsSync(arq)) return`, reportou sucesso sem abrir arquivo nenhum. Um verde que so prova que a lista encolheu. Licao codificada em `frontend/tests/unit/docs-integridade.test.js`, no teste "todo documento declarado em ALVOS existe no caminho declarado" (a lista passa a ser ASSERIDA, nao filtrada) e na troca do `return` silencioso do teto por um `expect(existsSync(...)).toBe(true)`; controle negativo executado nos dois sentidos (isca de caminho inexistente aqui dentro -> acusa `docs/livro-razao.md`; `ALVOS` revertido para a raiz -> acusa os dois alvos ausentes).
- 2026-08-14 `doc-sobre-codigo` [sessao] Herdei dos relatorios da auditoria do frontend o achado "o backend aceita um properties.source desconhecido que o CHECK valid_feature_type deveria recusar", cuja unica evidencia era o spec de e2e browser-military-tools reprovando ao esperar uma excecao no push. O backend nao aceita: desde 2026-07-25 (490b0c8f) a violacao de integridade (SQLSTATE 22/23) e capturada por operacao dentro de um SAVEPOINT e devolvida como recusa nomeada no ack de um lote 200, com log e efeito revertidos juntos, porque lote inteiro 400 congelava a fila do cliente para sempre. O spec era anterior aquela mudanca de contrato -> a conclusao "o servidor esta errado" veio da INTENCAO do teste e nao do caminho de codigo, sendo que a wiki ja descrevia o comportamento novo em duas paginas: teste vermelho aponta divergencia, nunca de que lado ela esta -> codificado no teste de integracao `backend/tests/integration/sync-unsupported-feature-source.test.js`, que observa contra Postgres real e distingue as tres explicacoes possiveis, e na expectativa reescrita do spec (que agora afirma a recusa nomeada, mais forte que a excecao, ja que excecao tambem seria satisfeita por 500 ou timeout). Regra que tiro: antes de escrever "o servidor aceita X", datar a mudanca de contrato contra a data do teste.
- 2026-08-14 `teste-que-nao-prende` [sessao] Ao remover o tileset semeado do catálogo, escrevi o teste do novo contrato como `assert.equal(cfg.tilesets.length, 0)`. Passou isolado, passou no controle negativo, e reprovou na suíte completa: `catalog-tables.test.js` cria um tileset por HTTP e não o remove, e a tabela é compartilhada -> contagem ABSOLUTA sobre tabela que outro teste escreve é frágil nos dois sentidos, e o pior é que ela parece mais rigorosa que a alternativa correta; eu tinha essa exata lição registrada quatro linhas acima (o `ranks === 19` de 2026-07-25) e reincidi mesmo assim, o que sugere que o registro em prosa não pega quem está escrevendo o assert -> troquei por asserção sobre o REGISTRO semeado (`find(t => t.id === 'PCL')` é `undefined`), que é o contrato de verdade: o que se proíbe é a migração semear, não a tabela ter linhas. Codificado no comentário do próprio caso, que é onde o próximo autor de assert vai olhar.
- 2026-08-14 `funcionalidade-dormente` [realidade] Ao portar a cena de primeira pessoa, o desvio de boot que manda um visitante logado para `atlas.html` por `window.location.replace` apareceu descartando o hash, e com ele o link inteiro. O defeito nao e novo: ele vale para `#view=3d` e `#view=360` desde que o desvio existe, e ninguem viu porque nenhuma das duas superficies tinha "compartilhar" como ferramenta de primeira classe, entao o caminho nunca era exercitado -> a licao nao e "conserte o roteamento", e que **funcionalidade dormente nao prova ausencia de defeito**: um caminho antigo, publicado e silencioso conta como nao verificado, nao como verificado, e a ausencia de relato so mede quantas pessoas passaram por ali; a feature nova nao introduziu o bug, ela virou o primeiro instrumento capaz de medi-lo -> codificado tirando a regra de `frontend/src/js/index.js` (que chama `initApp()` no import, entao nada dentro dele e alcancavel por teste) para `frontend/src/js/deep-link/route-decision.js`, com o porque no JSDoc da propria `shouldRouteToProjects`, e prendendo em `frontend/tests/unit/deep-link-fp.test.js` os casos das TRES gramaticas de hash mais o contra-exemplo do hash que nao nomeia viewer. Regra que tiro: ao tocar um caminho dormente, procure primeiro se ele ja estava errado, em vez de assumir que o silencio o validou.
- 2026-08-14 `verificacao-fantasma` [sessao] Ao conferir a alegacao de que `main` teria seis funcionalidades ausentes na branch, rodei `git grep -l <padrao> origin/main -- src` e ele devolveu VAZIO para os quatro arquivos em disputa. Escrevi "nao citado por nenhum arquivo de src em main" e ia publicar isso como prova de codigo morto. Rodei o controle positivo por reflexo, procurando um arquivo que eu SABIA estar vivo, e ele tambem veio vazio: o instrumento estava quebrado (combinacao de tree-ish com pathspec sob o shell que eu usava), e todas as quatro respostas eram do instrumento, nao do repositorio -> `grep` que devolve zero e indistinguivel de `grep` que nao rodou, e essa e a unica forma de saida que a busca produz **exatamente** quando a conclusao e "isto nao e referenciado por ninguem", que e a frase usada para JUSTIFICAR APAGAR codigo. A auditoria de exports mortos desta mesma semana removeu 7 arquivos inteiros sobre essa frase -> regra que passa a valer: toda alegacao de "X nao e referenciado" so vale acompanhada de um controle positivo NA MESMA execucao, com uma agulha sabidamente presente; resultado vazio sem agulha de controle e resultado descartado. Refeito com outra ferramenta, as quatro respostas mudaram de forma (os arquivos SAO citados, mas so pelo barril que os reexporta, que e o achado correto e mais forte).
- 2026-08-14 `verificacao-fantasma` [sessao] Ao medir D1 (onde esta o custo de um mapa com milhares de feicoes), o Playwright headless acusou ~95 ms de long task por `setData`, e eu ia reportar isso como o gargalo que justificaria tirar o store da thread da UI. A assinatura nao fechava: a DURACAO era constante (~95 ms com 0 e com 8000 feicoes) e so a CONTAGEM crescia com N, que e assinatura de quadro caro, nao de serializacao -> refeito com `headless: false` (GPU real), o mesmo experimento deu ZERO long tasks ate 8000: o bloqueio era o rasterizador de software do Chromium headless (SwiftShader), ou seja, o instrumento estava medindo a si mesmo, e o numero apontava para a conclusao que a proposta em analise queria ouvir -> regra que passa a valer: **medicao de RENDER nao se faz em headless**, e custo que nao escala com a variavel independente e suspeito de vir do instrumento, nao do sujeito. O controle que discriminou (30 s de app parado: zero long tasks, zero gaps de rAF, com 0 e com 8000 feicoes) era barato e devia ter vindo antes das quatro rodadas que gastei atribuindo o bloqueio a ponte MapLibre.
- 2026-08-14 `verificacao-fantasma` [sessao] Medi a alternativa ao `setData` (o diff incremental `updateData` do MapLibre 5.18, que o projeto nao usa em lugar nenhum) e obtive 0,3 ms contra 121 ms, plano em N, sem bloqueio: ia recomendar a troca com esse numero. Rodei o controle positivo por causa do tamanho do ganho, e ele derrubou a recomendacao: das 10 chamadas emendadas de `updateData`, so DUAS aplicaram (a primeira e a ultima), e esperar 6 s nao recuperava as outras oito, entao era perda de dado, nao leitura prematura. Com 300 ms de folga entre chamadas, ou com as 10 num lote unico, 10/10 -> **ganho grande demais e motivo para duvidar do proprio numero**: `updateData` retorna barato porque delega ao worker, e chamada emendada sobrescreve a anterior; recomendar a troca crua nas 293 chamadas de `setData` teria trocado 121 ms de jank por perda silenciosa de feicao do usuario -> a via segue valendo, mas exige despachante coalescente por source (um diff por vez, acumulando enquanto o anterior nao voltou), e a condicao de habilitacao tambem foi medida: `promoteId: 'id'` na declaracao da source (a opcao `dynamic` nao existe nesta versao, o proprio MapLibre responde `unknown property`).
- 2026-08-14 `verificacao-fantasma` [sessao] O despachante de diff nasceu declarando a propria pre-condicao no `@fileoverview`: uma source so pode ser migrada quando TODOS os seus escritores passarem por ele, porque um `setData` cru substitui o slot de pending-update do MapLibre e o diff enfileirado some sem erro nenhum. A fase 1 migrou 24 ferramentas e a pre-condicao ficou VIOLADA no mesmo commit: 22 escritores crus vivos sobre sources ja migradas, incluindo `setOrCreateSource`, que e o co-escritor de todas as dezesseis -> regra que so existe em prosa nao e regra, e esta era invisivel na leitura do arquivo que a cometia (quem escreve `getSource('polygons').setData(fc)` num controle de medicao nao tem como saber ali que outro arquivo migrou `polygons` tres meses antes) -> codificada em `frontend/tests/unit/despachante-sem-escrita-crua.test.js`, varredura estatica por AST que reprova toda escrita cujo alvo nao se PROVA fora do inventario migrado, com o inventario DERIVADO dos sitios de `getGeoJsonDispatcher` (nunca enumerado a mao) e a allowlist carregando motivo E contagem de chamadas por arquivo, para que a proxima escrita crua num arquivo ja isento tambem quebre. Controle positivo embutido (fixtures sinteticas pela MESMA funcao de analise) porque varredura que deixa de casar passa verde para sempre; controle negativo executado por dois caminhos (escrita crua reintroduzida em `polygon.layers.js` -> acusa; chamada extra num arquivo isento -> acusa a contagem). Fecharam 16 co-escritores; ficaram 5 arquivos na allowlist, dos quais so `temporal-render.service.js` ainda toca source migrada.
- 2026-08-14 `cobertura-vazia` [sessao] O modulo do despachante de diff existe para uma coisa so: impedir que dois `updateData` se sobreponham na mesma source, que e a interleaving medida como perda de dado. O arquivo de teste dele ANUNCIAVA no cabecalho provar exatamente isso ("no maximo UM updateData em voo por vez"), tinha 31 casos, e o duplo de source ate contava `maxInFlight`. Removido o `if (this._pumping) return this._pumping` de `_pump`, a suite INTEIRA do frontend passava: 3407 de 3407, com a serializacao de voo deletada -> nenhum caso produzia a sequencia que sobrepoe (`flush()` chegando com um diff em voo, que e o que `features_tab.js` e `label-tab.helpers.js` fazem de verdade), entao o contador existia e nunca chegava a contar dois; o cabecalho agravou, porque declarar a prova faz o proximo leitor parar de procurar por ela. Pego pelo portao da rodada, plantando o defeito em vez de ler o teste -> codificado no caso "flush concorrente com um diff em voo nao sobrepoe updateData", com pre-condicao explicita (`inFlight === 1` antes de agir, senao o verde poderia vir de nunca ter havido voo nenhum); controle negativo confirma que ele e o UNICO dos 32 que cai com o invariante removido. Regra que tiro: teste que anuncia prender um invariante deve ser sondado plantando o defeito, e o alvo da sonda e a linha que implementa o invariante, nao a que o descreve.
- 2026-08-14 `verificacao-fantasma` [sessao] A auditoria que acompanhou a serializacao das escritas do store afirmou "nao sobrou nenhum escritor do documento do mapa fora de trava", e a afirmacao era falsa: `renameMap` no caminho UUID (`frontend/src/js/store/repositories/local.repository.js`) le o documento inteiro, muta `name` e grava de volta, e ficou destravado. A auditoria grepou `updateMapDataCompat` e `repo.saveMap`; o rename grava por `mapStore.setItem`, que nao casa com nenhum dos dois padroes -> e a forma classica de conferir um subconjunto e tratar como o conjunto, agravada porque a justificativa escrita para excluir o ciclo de vida ("substitui o registro inteiro em vez de fazer RMW") era verdadeira para `addMap`/`removeMap` e falsa justamente para o terceiro -> medido pelo agente refutador em 0 de 20 execucoes corretas, nas duas ordens (renomear um mapa enquanto o usuario desenha descarta uma das duas escritas). Corrigido com `withMapDocument` em volta do vao, e preso por dois casos em `frontend/tests/store/map-operations.test.js`: um confirma que o rename roda DENTRO da trava, outro prova a exclusao pelo efeito (nenhuma secao comeca antes de a anterior terminar), deliberadamente sem assertar a ORDEM de chegada, que prenderia o escalonador e nao a exclusao. Regra que tiro: auditoria de "todo escritor de X" se faz pelo destino da escrita (quem grava em `mapStore`), nunca pela lista de funcoes intermediarias que se lembra de nomear.
- 2026-08-14 `verificacao-fantasma` [sessao] O `upload-negado-perde-a-resposta.repro.test.js` do backend vinha falhando de forma intermitente havia duas sessoes e foi rotulado de flake de ambiente as duas vezes, inclusive por mim, com a medicao "1 falha em 8" servindo de justificativa para seguir adiante. Medido direito agora: 8 de 8 verde com o arquivo ISOLADO e 2 falhas em 4 execucoes da SUITE COMPLETA, o que ja descarta ambiente e aponta para o instrumento -> a causa e ambiguidade do proprio teste: o caso do chamador anonimo aferia o SINTOMA (a conexao cai, entao nao ha status), e a queda so acontece enquanto o corpo ainda sobe; a primeira clausula de `drainOnError` (`req.complete`) vem ANTES do teste de anonimo, entao sob carga o corpo de 3 MB chega inteiro, o middleware passa direto por ali e a resposta sai normalmente. Os dois motivos de "nao drenou" produzem desfechos opostos e o teste lia so um -> "flake" e o rotulo que faz parar de investigar, e ele custou duas sessoes; um vermelho conhecido e recorrente treina a equipe a ignorar vermelho, que e o dano real. Codificado em `backend/tests/unit/drain-on-error.test.js`, que exercita a DECISAO sem socket, sem carga e sem corrida (o middleware recebe um request de mentira e o que se afere e SE ele leu o corpo), e o caso de integracao foi desamarrado da queda, passando a afirmar so o que e estavel em qualquer escalonamento. Regra que tiro: teste que afere consequencia de corrida afere o escalonador; a decisao se afere onde ela e tomada.
- 2026-08-14 `verificacao-fantasma` [sessao] Ao escrever o teste acima, o helper montava o request falso com `stream.on('data', ...)` para saber se o middleware tinha lido o corpo. Tres dos quatro casos ficaram vermelhos acusando leitura que nao houve: registrar um listener de `data` poe o Readable em modo FLUENTE, entao o instrumento consumia o corpo e depois culpava o sujeito por te-lo consumido -> e a mesma classe do `import()` que recebe outra instancia do modulo, e da sonda que pergunta pelo estado alterando o estado: aqui o ato de observar era o ato de consumir -> trocado por um `Readable` com `_read` instrumentado, que so dispara quando alguem realmente pede dados. Pego rapido porque o vermelho foi grande demais para ser plausivel, e essa e a heuristica que vale registrar: quando o instrumento acusa quase tudo, desconfie do instrumento antes do sujeito.
- 2026-08-15 `contrato-implicito` [sessao] Removida a espera do predicado do tab-lock (o ramo que fazia dois atlas de servidor QUAISQUER colidirem), o refutador anterior tinha deixado um caso aberto que ninguem fechou: depois de `adoptRemoteAtlasAsLocal`, o slot LOCAL guarda o sufixo `remote-<atlasId>`, isto e, os mesmos dez bancos do atlas de servidor, e `keysCollide` respondia `false` porque comparava (kind, atlasId) enquanto o que esta em disputa e o ENDERECO dos bancos -> o predicado passou a computar um endereco (`claimAddress`) e a chave local ganhou `adoptedFrom`, preenchido em `currentAtlasLockKey` a partir do sufixo do escopo (`remoteAtlasIdFromDbSuffix`), de modo que o slot resgatado colide com o atlas de onde veio sem deixar de ser local para o usuario e para o overlay -> a regra que tiro e a que o proprio fileoverview do modulo ja afirmava e o codigo nao cumpria: quando a doc diz que se arbitra um ENDERECO, o predicado tem de comparar o endereco, e nao o rotulo que costuma acompanha-lo. Codificado em `frontend/tests/unit/tab-lock.test.js` (ROW 6), `frontend/tests/unit/tab-lock-refutacao.test.js` (2.1b) e nos dois testes de integracao, o de `namespace-remoto-fiacao` com a adocao rodando sobre a fabrica de namespace real.
- 2026-08-15 `doc-sobre-codigo` [sessao] Auditei a wiki contra o codigo depois da fase de namespace por atlas e achei o dobro do que a pendencia previa: nao eram cinco linhas em quatro paginas, eram dez afirmacoes em oito, e as mais caras nao vinham de descuido, vinham da FORMA. Duas paginas escreveram no presente uma lista de furos abertos e o pipeline de uma fase em curso ("a fila de saida fica global porque o envelope nao carrega id de atlas", "o link publico ativa e o proprio wipe desfaz", "a trava remoto x remoto saiu por ultimo"), e o trabalho da semana seguinte fechou os itens sem tocar na doc -> conteudo cuja validade vence por trabalho ALHEIO nao pode morar na wiki, porque o autor da correcao nao passa por la, e `docs-integridade` valida caminho e simbolo, nunca a afirmacao: as dez linhas falsas estavam verdes o tempo todo. O dano nao e hipotetico, essa fase o pagou duas vezes (planejamento partiu de "a fila e global" e de "dois atlas remotos nao coexistem"). Um caso foi ao contrario e vale igual: a doc dizia que abrir link publico DESCARTA o desenho local anonimo, e o codigo ja nao descartava, entao a pagina assustava o usuario com uma perda que nao existe mais -> codificado como regra de forma em `docs/wiki/wiki-schema.md` ("lista de furos ABERTOS nao mora na wiki": pendencia e do arquivo de trabalho da fase, e limite permanente se escreve como propriedade do desenho), mais a secao `## Historico` das paginas corrigidas. Regra que tiro: ao fechar uma etapa, a busca na doc nao e pelo NOME do que mudou, e pelas frases que a mudanca torna falsas.
- 2026-08-15 `doc-sobre-codigo` [sessao] Segunda passada da wiki no mesmo dia, e o achado nao foi uma linha antiga: foi uma linha CORRIGIDA de manha que envelheceu a tarde. `namespace-por-atlas` dizia "o protocolo do tab lock nao tem mensagem de desmontagem" e `coordenacao-entre-abas` ja descrevia `announceTeardown` funcionando, porque o aviso entrou no codigo cerca de vinte minutos depois de a primeira pagina ser revisada -> duas paginas da MESMA wiki afirmando o contrario uma da outra, e nenhum guarda ve isso: `docs-integridade` valida caminho e simbolo, e o `lint-wiki` mede vocabulario (Jaccard), nunca a afirmacao. Pior, o arquivo de pendencias da fase (`_PENDENCIAS.md`, P10) dizia que o aviso NAO tinha sido feito, entao conferir a doc contra a nota de pendencia teria confirmado a frase falsa com ar de verificacao: a nota descreve o mundo do dia em que foi escrita, e numa fase com quatro frentes em paralelo isso e ontem. Regra que tiro, complementar a de forma da linha acima: em fase em curso a doc se confere contra o CODIGO no dia em que se escreve, e depois de tocar uma peca em construcao vale grep pelas frases que a peca torna falsas em TODAS as paginas, nao so na que se estava editando. Codificado no `## Historico` de `docs/wiki/namespace-por-atlas.md`.
- 2026-08-15 `contrato-implicito` [sessao] A varredura que destrói os namespaces de atlas de servidor pendurava numa condicional `!isAuthenticated` DENTRO de `clearAllDataStore`, ou seja, o apagamento decidia o próprio alcance perguntando ao mundo em vez de recebê-lo. O efeito era um wipe que virava logout por conta própria: o visitante de link público registrava o namespace e, três linhas abaixo, o mesmo wipe o destruía, de modo que o snapshot público caía no slot local do usuário -> a regra que tiro vale para todo código destrutivo: **o que um wipe destrói é ARGUMENTO, nunca inferência sobre o estado do mundo**, porque a inferência acerta no caminho para o qual foi escrita e erra em todos os outros, sem erro nenhum. Codificado separando os papéis (`clearAllDataStore` recebe alvo; `discardRemoteAtlasNamespaces` é chamada POR NOME nos dois pontos que significam "a sessão acabou") e preso em `frontend/tests/unit/wipe-unificado-de-atlas.test.js`, com o par de casos DESLOGADO/LOGADO afirmando que os dois se comportam igual, que é a asserção que só é possível depois de a sessão deixar de ser consultada.
- 2026-08-15 `teste-que-nao-prende` [realidade] O caso que policiava "o wipe de atlas não encosta no banco GLOBAL da instalação" semeava a chave LEGADA `local_atlases`, que produção já não escreve desde que o registro virou uma chave por slot. Ele passava, e não podia deixar de passar: afirmava a sobrevivência de um dado que nenhum caminho de produção cria, então o banco global podia ser esvaziado inteiro sem o caso mudar de cor -> **teste que semeia o estado errado é cobertura vazia com cara de cobertura**, e a forma de descobrir é perguntar quem ESCREVE aquilo que o teste está lendo, não se o teste está verde. Codificado em `frontend/tests/unit/repository-namespace.test.js`, que passou a semear pela chave real (`localAtlasDiskKey`) e a ler pelo helper compartilhado, com o comentário registrando qual chave a versão anterior usava.
- 2026-08-15 `verificacao-fantasma` [sessao] A troca do registro local (um array sob `local_atlases` para uma chave por slot) deixou 31 casos vermelhos sem UM bug no código: todos liam o layout direto do disco falso. A correção óbvia era extrair um helper que delegasse ao leitor de produção (`readLocalAtlasRegistry`), e ela teria destruído o instrumento: sujeito e medição concordariam POR CONSTRUÇÃO, e um defeito dentro daquele leitor ficaria invisível em 31 casos de uma vez -> **instrumento que chama o sujeito não mede nada**, e a saída não é duplicar em silêncio, é duplicar DECLARANDO: o helper reimplementa só os nomes de chave, como segunda afirmação independente do contrato de disco, e um caso separado prende as duas afirmações uma na outra para que a divergência falhe alto. Codificado em `frontend/tests/helpers/atlas-registry-disk.js` (o porquê está no `@fileoverview`) mais o caso de `frontend/tests/unit/atlas-namespace.test.js` que compara as constantes do helper com `GlobalKey`.
- 2026-08-15 `premissa-inventada` [sessao] Os quatro degraus de migração abriam os bancos por NOME FIXO (`ebgeo_maps` e irmãos), o que era correto enquanto a instalação tinha um conjunto de bancos só. Com namespace por atlas isso deixou de ser "o atlas do usuário" e passou a ser "os bancos pré-namespace", que podem não ser os do slot montado: a migração rodaria sobre o banco errado sem lançar nada -> são DOIS trabalhos com o mesmo nome (atualizar a INSTALAÇÃO e atualizar UM SLOT), e o que os separa não é uma flag, é o alvo virar argumento; inferi-lo do escopo ativo confunde os dois exatamente quando eles diferem. Codificado em `frontend/src/js/store/migration/migration-scope.js` (`legacyScope`/`isLegacyScope`), com `safelyMigrate` e `migrateActiveSlot` recebendo o escopo, verificado sobre fixture real de um usuário 2.2 em `frontend/tests/integration/migracao-22-para-23-fixture-real.test.js` (incluindo o controle de que origem REMOTE não vira cópia local), e a etapa 3 de §Adding a Schema Migration em `.claude/rules/common-tasks.md`.
- 2026-08-15 `verificacao-fantasma` [realidade] `clearAtlasDatabases` esvaziava chamando `clear()` em cada banco do escopo, e `clear()` passa pelo `ready()` do localforage, que ABRE o banco: abrir um nome que não existe o CRIA. Então esvaziar um escopo nunca escrito fabricava os dez bancos e relatava os dez como destruídos, e `purgeReachedAtlas` respondia `true` para um atlas que nunca esteve lá, o que fazia a guarda de boot PULAR o wipe do atlas que estava mesmo montado -> **um destruidor que fabrica o que diz destruir é um verificador que mente**, e a assinatura é a mesma da sonda que altera o estado que veio ler; a saída não foi trocar de fonte de verdade (a tentativa por `indexedDB.databases()` foi revertida por discordar do dublê que as suítes instalam), foi pedir o sinal pelo MESMO caminho da escrita (`keys()`) e separar "esvaziei" de "abri um vazio" no relatório. Codificado no retorno `{names, cleared}` de `clearAtlasDatabases` (`frontend/src/js/store/atlas-namespace.js`) e no `databaseState()` de três respostas (`absent`/`empty`/`populated`) de `frontend/tests/helpers/idb-helpers.js`, que nunca abre um nome que não esteja em `indexedDB.databases()`.
- 2026-08-15 `contrato-implicito` [sessao] O registro remoto promete que a identidade mora na CHAVE e não no valor, para que um valor corrompido não esconda um atlas de servidor da varredura. A promessa não cobria a chave em si: um id que `remoteScope` recusa fazia o `throw` escapar da varredura INTEIRA, e uma única chave estragada deixava a máquina deslogada com TODOS os atlas de servidor no disco, que é o oposto exato do invariante mais duro do store -> quando um laço percorre entradas que vieram de disco, a pergunta não é "o valor pode estar corrompido", é **"o que acontece com as outras N-1 entradas quando a entrada k é impossível"**; falha por item tem que ser contida no item, e o item que não nomeia namespace alcançável não custa nada ao ser pulado. Codificado em `purgeAllRemoteAtlases` (`frontend/src/js/store/remote-atlas.api.js`), que pula a entrada ilegível com erro no console e a mantém no disco em vez de escondê-la, e preso pelos três casos de "uma entrada corrompida não derruba a varredura" em `frontend/tests/unit/remote-atlas-api.test.js`.
- 2026-08-15 `verificacao-fantasma` [realidade] Uma rodada de `browser-multi-tab-namespace.spec.js` deu seis vermelhos em dez casos, com quatro sintomas diferentes ao mesmo tempo (`localforage is not defined` num módulo servido com `?t=<epoch>`, dois "Execution context was destroyed" em testes que não navegam, e um `Failed to fetch` no seeding), e a mesma rodada minutos antes e minutos depois veio limpa. A causa não era do produto: o dev server do e2e observa `src/`, e uma edição feita por outra frente durante a rodada reinjetou os módulos e recarregou a página no meio da medição -> é a armadilha do instrumento medindo outra cópia do sujeito na forma mais grosseira, a página sob medição sendo TROCADA durante a medição, e o diagnóstico veio da assinatura (`?t=` no stack) e não da taxa, porque vermelho intermitente é lido como flake por padrão -> codificado em `frontend/tests/e2e-ui/vite.e2e.config.js` (`hmr: false` mais `watch: null`, que removem o canal em vez de silenciá-lo, e o `@fileoverview` argumenta por que isso não mascara corrida: nada em `tests/e2e-ui/` edita fonte), com o CONTROLE no caso A0z do próprio spec, que afirma que a aba não abriu WebSocket de volta para a origem do app nem buscou módulo com `?t=`, porque sem controle "não houve reload" e "houve reload, mas não desta vez" se leem igual.
- 2026-08-15 `contrato-implicito` [realidade] O aviso de desmontagem entre abas (TEARDOWN) mandava a aba avisada SOLTAR o lock de montagem ao congelar, com o argumento de que uma aba parada mas ainda montada so empurraria a destruicao para o prazo de 24 h. O lock nao era contabilidade sobre QUANDO: era a arbitragem, a unica coisa entre os bancos da vizinha e o expurgo do logout alheio. Medido pela auditoria, com controle: com a montagem viva o relatorio diz `spared` e o dado e a fila de saida (`perAtlas`, logo dentro do namespace condenado) sobrevivem; sem ela, `atlases`, dado nulo e banco de fila ausente -> ou seja, **obedecer ao aviso era o que destruia a aba que o aviso existe para proteger**, e essa aba nao tem resgate nenhum, porque `preserveUnsyncedWorkAsLocal` so roda em quem desloga. A regra que tiro: aviso e INFORMACAO, e quando o efeito de um aviso inclui soltar a protecao que ja existia, o aviso virou entrega; a pergunta certa e "existe estado em que o vizinho para de escrever E continua protegido", e quando a resposta custa alguma coisa dos dois lados, a escolha entre as duas perdas se escreve no codigo em vez de ficar implicita (aqui: dado de servidor sobrevive ao logout enquanto aquela aba viver, limitado pelo prazo, contra trabalho nao enviado destruido sob uma aba viva, limitado por nada). Codificado em `applyTeardownFreeze` (`frontend/src/js/store/sync/tab-lock-sync-brake.js`), que so solta no regime SEM `navigator.locks` (onde poupar e impossivel por construcao), e preso em `frontend/tests/unit/tab-lock-sync-brake.test.js`, cujos tres casos que exigiam o mundo antigo foram TROCADOS, nunca somados: um teste que segue cobrando o comportamento antigo faz a correcao parecer regressao.
- 2026-08-15 `verificacao-fantasma` [sessao] O mesmo aviso so existia num dos DOIS caminhos que rodam a varredura destrutiva: estava escrito dentro do logout, e a guarda de boot deslogado chamava a mesma varredura sem avisar ninguem, entao o ramo `forced` (prazo vencido) destruia o namespace de uma aba viva em silencio. O conserto obvio, uma linha chamando o aviso tambem no boot, teria sido FANTASMA: `announceTabLockTeardown` nao criava o lock, e `index.js` so chama `initTabLock` DEPOIS do boot da store, entao a chamada devolveria um relatorio bonito sem postar mensagem nenhuma, e um teste que aferisse o retorno ficaria verde -> duas regras. Uma: quando dois caminhos correm o mesmo trecho destrutivo, o guarda pertence ao TRECHO (hoje quem avisa e `discardRemoteAtlasNamespaces`), porque guarda preso ao chamador so protege os chamadores que alguem lembrou. Outra: teste de aviso afere o que a VIZINHA recebeu, nunca o que a funcao devolveu. Codificado em `announceRemoteNamespaceTeardown` (`frontend/src/js/store/store.js`, derivacao unica da lista, exclusao do slot resgatado inclusa), no participante descartavel de `announceTabLockTeardown` (`frontend/src/js/utilities/tab-lock.js`, chave `none`, sem overlay, descoberta de vizinho por TURNOS do laco de eventos e nunca por `setTimeout`, que travaria os harness que congelam o relogio) e em `frontend/tests/unit/wipe-unificado-de-atlas.test.js`, onde a vizinha e um `TabLock` de verdade no canal real e o caso mede tambem QUANTOS bancos ainda tinham dado no instante do aviso, porque avisar depois de esvaziar e nao avisar.
- 2026-08-16 `verificacao-fantasma` [sessao] As duas `real-fixtures.js` gemeas (frontend e backend) exportavam `ALL_FEATURE_SOURCES` com 18 tipos e o comentario "the 18 backend-valid feature types", enquanto o Joi, a CHECK do banco, o cliente e a store concordavam em VINTE: faltavam `sector` e `magnetic_declination`. Um `assert.equal(ALL_FEATURE_SOURCES.length, 18)` e dois specs intitulados "every feature type" varriam esse subconjunto e reportavam sucesso -> **fixture que afirma completude sobre um subconjunto e a copia mais perigosa do repositorio, porque veste roupa de verificacao**, e o assert literal nao detecta o buraco, ele o CONGELA e ainda anuncia o numero errado a quem le o verde. As duas listas passaram a ser DERIVADAS, cada uma da autoridade do proprio pacote (chaves de `FEATURE_TYPE_MAPPINGS` no frontend, `VALID_FEATURE_TYPES` extraido de `atlas.schemas.js` no backend), com piso que LANCA no load, e as varreduras fecharam contra autoridade VIVA em vez de contra um numero: a CHECK do banco (`pg_get_constraintdef`) no backend, e o conjunto de baldes do snapshot do servidor no e2e. Uma armadilha medida no caminho, contra a propria sugestao do plano: derivar por `getAllStorageTypes()` e voltar com `getSourceTypeFromStorage` valida um tipo INEXISTENTE, porque `getStorageTypeFromSource` cai no fallback `source + 's'` e faz 'setore' fechar a volta com 'setores' -> quando o par de funcoes tem fallback, ida e volta nao e prova de identidade; derive da chave, que ja e a resposta. Codificado em `frontend/tests/helpers/real-fixtures.js`, `backend/tests/helpers/real-fixtures.js`, `backend/tests/integration/features-real-shape.test.js`, `frontend/tests/e2e/feature-types-all.e2e.test.js` e `frontend/tests/e2e-ui/browser-collab-all-types.spec.js`.
- 2026-08-16 `teste-que-nao-prende` [sessao] O censo de tipos de feicao nasceu exigindo que TODA entrada continuasse aparecendo na varredura de literais, e derivar um arquivo do registro APAGA os literais dele: a migracao bem-sucedida ficaria vermelha, e o caminho facil para consertar o vermelho seria apagar a entrada, perdendo justamente o registro de quem deriva -> **guarda que pune o resultado que ele existe para produzir**. So apareceu ao rodar o controle positivo, nunca por leitura. A forma que sobreviveu tem dois estratos, porque um so nao sustenta o controle: cada entrada declara COMPLETA (promete carregar todos os nomes, cobrado por presenca) ou SUBSET (recorte deliberado, com motivo escrito), e quem deriva ganha isencao da varredura mais um caso irmao que exige o import de verdade. Uma varredura de "declare ou derive" sozinha nao olha para o conteudo do registro, entao uma linha falsa acrescentada la passaria verde. Codificado em `frontend/tests/unit/registro-tipos-cobertura.test.js` e na decisao de 2026-08-16 em `docs/decisions/decisions-2026.md`.
- 2026-08-16 `verificacao-fantasma` [sessao] A migracao 2.2 -> 2.3 tinha 22 casos verdes e nenhum deles rodava num navegador: todos sobre `fake-indexeddb`. Um duplo em processo nao tem `Blob` (o proprio harness DECLARA a limitacao, porque sem `FileReader` o localforage cai em `_encodeBlob` e a escrita lanca) e nao tem duas abas disputando os mesmos bancos, que e exatamente o que uma migracao faz aparecer -> **um duplo responde "a logica esta certa", nunca "o usuario de producao atualiza sem perder nada", e so a segunda pergunta importa no dia do deploy**. Duas armadilhas medidas ao escrever os casos. Uma: semear DEPOIS do primeiro boot mede uma fusao que nenhum usuario vive, porque o app ja adotou o slot #1 e escreveu um mapa em branco; a semeadura foi para uma pagina em branco da mesma origem, e so entao `goto('/')` vira o ato sob medicao. Outra: o banco de imagens tem DOIS habitantes, e confundi-los custou uma investigacao inteira -- alem das do arquivo, `regenerateIcon` regrava o diagrama de declinacao magnetica a cada desenho, chaveado pelo ID DA FEICAO, entao a assercao correta e assimetrica (nada do arquivo pode faltar, e o que sobrar tem de ser id de feicao de declinacao), e uma contagem simples teria virado um numero magico. O controle negativo tambem ensinou onde o guarda mora: mutar o `adoptLegacyDatabases: true` EXPLICITO da migracao nao derruba nada, porque `initLocalAtlases` ja rodou no boot antes dela; so mutando o ponto real de adocao os tres casos ficam vermelhos. Codificado em `frontend/tests/e2e-ui/browser-migracao-2.2.spec.js`, com o layout de chaves partido em `buildLegacyEntries` (`frontend/tests/helpers/ebgeo-fixture.js`) para que navegador e node semeiem da mesma fonte.
- 2026-08-16 `verificacao-fantasma` [sessao] A suite de Playwright ficou 41 minutos VERDE em 249 casos e VERMELHA em 12, e ninguem sabia, porque ela roda fora do `npm test` e e cara: onze das doze afirmavam contratos que o produto trocou DE PROPOSITO semanas antes (recusa por operacao em vez de 400 abortando o lote, ressuscitar-na-recriacao para o Ctrl+Z parar de perder dado, tileset semeado removido do catalogo, login virando navegacao) -> **teste que afirma o mundo antigo nao envelhece em silencio, ele passa a ACUSAR a correcao de ser regressao**, e cada um deles foi TROCADO, nunca somado. Duas licoes vieram de dentro dessa limpeza, e valem mais que o placar. A primeira: `drawViaToolUI` esperava a ferramenta ficar ativa com `getControl(toolId)`, mas o registro e chaveado por NOME DE CLASSE (`AddPointControl`) e o helper passava o id da barra (`point`), entao o predicado nunca podia ser verdadeiro, o timeout de 5s sempre estourava e um `.catch(() => {})` engolia; o comentario acima dele descrevia a garantia como se ela existisse, e a protecao real era um `sleep(150)`, que e exatamente por que os desenhos falhavam sob carga e passavam isolados -> quando a espera tem catch vazio, ela nao e espera, e um comentario que descreve garantia inexistente e pior que nenhum. A segunda: eu mesmo escrevi uma espera nova cujo LIMITE declarado ("um atlas cujo mapa inicial se chame Principal") era alcancavel por um spec que ja existia, e ela reprovou comportamento correto na rodada seguinte; o conserto foi trocar o discriminador de NOME para CHAVE (mapa local e chaveado pelo nome, mapa de atlas por UUID) -> limite que voce consegue enunciar e limite que alguem ja atingiu; se da para escolher um discriminador exato, escolha o exato. Codificado em `frontend/tests/e2e-ui/helpers/collab-helpers.js` (espera por sinal real do tool manager, sem catch, e `currentMapKeyIsUuid`) e nos onze specs corrigidos.
- 2026-08-16 `teste-que-nao-prende` [sessao] Dois defeitos de PRODUTO vivos estavam escondidos atras de testes de navegador que ninguem rodava, e os dois so aparecem quando se CONTA. Um: `_buildGeoJSON` lia `tileset.locate.lon` sem guarda enquanto o ramo irmao tres linhas abaixo filtrava com `Number.isFinite`, e o catalogo edita `config` como JSON LIVRE, entao um tileset salvo sem posicao derrubava a camada de marcadores inteira e desabilitava os Modelos 3D para TODOS os usuarios, sem nada na tela. Dois: `briefing_edit_started` e `briefing_edit_ended` eram os dois ultimos frames de percepcao sem `clientId`, e a razao ja estava escrita por extenso no `handleCursor` do mesmo arquivo -- o roster e CHAVEADO por clientId, entao frame so com userId nao atualiza a entrada, ele CRIA UMA SEGUNDA -> quem editava briefing aparecia duas vezes na lista de quem esta online. O caso ja asseria o texto "ausente" e passava, porque o texto estava na linha FANTASMA; so uma assercao de CONTAGEM de linhas separou "marcou o par" de "inventou um par". A regra: **assercao de presenca aceita duplicata; quando a identidade importa, conte**. Codificado em `frontend/src/js/3d_models_viewer_tool/add_3d_models_viewer_control.js`, `backend/src/modules/collab/collab.handlers.js` e na contagem de linhas de `frontend/tests/e2e-ui/presence.spec.js`.
- 2026-08-16 `verificacao-fantasma` [sessao] Duas manifestacoes da mesma raiz no mesmo lote, e a raiz e "a verificacao nao alcancou a ultima escrita". Uma: traduzi as mensagens Joi para pt-BR, rodei a suite INTEIRA do backend verde, e depois acrescentei a traducao do `db_filename` sem re-rodar; o vermelho (um teste que ancorava a regex `/basename/` na frase em ingles) so apareceu quando um agente do workflow rodou a suite horas depois -> a suite que vale e a que rodou DEPOIS da ultima escrita, e "eu ja rodei" e sempre sobre um estado anterior. Outra: para esperar a suite da raiz terminar, escrevi um `until grep -qE "e2e|Coverage summary"`, que casou a linha `> npm run test:e2e` que o npm imprime ao COMECAR a perna, e o monitor anunciou pronto com o e2e ainda rodando -> **todo log anuncia o que vai fazer antes de fazer**, entao predicado ancorado em texto de log mede o anuncio; o certo e o codigo de saida do processo. Codificado nos dois novos marcadores de §Verificacao em `CLAUDE.md` (o quarto episodio de "o verificador quebra calado", agora com a forma "predicado casou o anuncio", mais a regra de que a suite valida e a posterior a ultima escrita).
- 2026-08-16 `teste-que-nao-prende` [sessao] O spec e2e-ui recem-escrito para "o segundo boot nao reimporta o .ebgeo" comparava `expect(depois.idsDeMapa).toEqual(estado.idsDeMapa)` sem nenhuma assercao absoluta sobre o primeiro conjunto: dois arrays VAZIOS satisfazem a igualdade, entao um repositorio apontado para o slot errado passaria verde afirmando exatamente o contrario do que o caso promete. O engano e sutil porque o arquivo TEM assercoes absolutas fortes logo acima (onze mapas por nome), mas elas medem a UI (`getAllMapNamesStore`) e o controle compara outra fonte (`getRepository().getAllMapIds()`) -> **assercao absoluta numa fonte nao cobre o vazio de outra**, e o par "compare dois conjuntos" so tem sentido depois que um deles foi ancorado por numero. Codificado no `expect(estado.idsDeMapa.length).toBe(11)` de `frontend/tests/e2e-ui/atlas-local-ebgeo-e-teardown.spec.js`, com o comentario nomeando as duas fontes para o proximo leitor nao achar que a assercao e redundante.
- 2026-08-16 `contrato-implicito` [sessao] Renomeei "Salvar no servidor" para "Enviar ao servidor" na aba Mapas e so ali, porque era o botao que a tarefa mencionava. O resultado foi TRES nomes vivos para uma acao: o novo na aba, o antigo no menu da conta (que e por onde um spec de e2e a dirige), e duas mensagens ao usuario instruindo `use "Salvar no servidor"`, apontando para um botao que aquele nome ja nao tinha. A revisao adversarial pegou; leitura do diff nao pegaria, porque cada arquivo estava internamente coerente -> **rotulo e dado replicado sem guarda**: a constituicao ja exige um nome para uma coisa, e nada no lint le string de UI, entao trocar rotulo e uma operacao de VARREDURA no repositorio inteiro (fonte, teste, wiki), nunca uma edicao no ponto que se estava editando. Corrigido em 24 ocorrencias (11 em `frontend/src/js`, 8 em testes, 5 na wiki), com a varredura final de `Salvar no servidor` devolvendo zero fora de `dist/`.
- 2026-08-16 `teste-que-nao-prende` [realidade] Escrevi o caso "quem nao tem acesso nao ve o projeto no overview" com tres `assert.equal(..., undefined)` e um `beforeEach` que APAGAVA a capa antes de cada caso: os tres undefined eram verdadeiros por ausencia de dado, nao por filtro de escopo, entao o caso passava verde com o predicado de acesso removido. Nao apareceu na leitura nem na suite verde; apareceu no controle negativo, que mutou os DOIS defeitos de uma vez e devolveu UM vermelho onde eu esperava dois -> **o controle negativo nao serve so para confirmar que o guarda pega, serve para CONTAR quantos ele pega**, e a diferenca entre o esperado e o medido e o unico sinal de cobertura vazia que chega sozinho. O conserto foi gravar a capa e afirmar que o dono a recebe (premissa asserida) antes de negar ao estranho. Codificado em `backend/tests/integration/atlas-cartao-projeto.test.js`, com o comentario dizendo por que a premissa esta ali para o proximo leitor nao a achar redundante.
- 2026-08-16 `contrato-implicito` [realidade] O dono perguntou "faz sentido esta interface?" sobre um dialogo que dizia "Abrir este projeto do servidor substitui os dados que estao abertos agora", com botao vermelho "Descartar e abrir". A resposta so apareceu porque MEDI em vez de ler: desenhei um ponto no mapa local, cliquei no botao vermelho, voltei e o ponto estava la -> **o aviso ameacava uma destruicao que a mudanca de arquitetura tinha eliminado quatro semanas antes**, e nenhum teste ficou vermelho porque nenhum afirmava a consequencia, so o fluxo. A classe: quando o namespace por atlas passou a mirar o wipe no atlas ABERTO, todo texto de UI que falava do wipe virou mentira, e texto de UI nao tem guarda nem lint. Um botao destrutivo que nao destroi e pior que botao nenhum, porque ensina a atravessar aviso. Codificado em `frontend/tests/e2e-ui/abrir-servidor-preserva-local.spec.js`, que afirma as DUAS metades (nenhuma pergunta E o dado local intacto: a primeira sozinha passaria verde num openRemoteAtlas que voltasse a apagar o slot), com controle negativo medido invertendo a ordem `activateRemoteAtlas`/`clearAllDataStore`.
- 2026-08-16 `contrato-implicito` [realidade] Centralizei a escrita das preferencias de aparencia do atlas num servico e o extrator de `compactacao-id-nao-unico.test.js` EMAGRECEU sem eu perceber: ele varria `logSettingOperation(a, b, { chave: ... })` como literal inline, e o servico monta o patch a partir de uma lista, entao `terrainExaggeration` sumiu do inventario junto com a chave nova. O teste ficou vermelho pelo motivo certo (a lista mudou) e pelo errado ao mesmo tempo (a extracao encolheu), e so o PISO de contagem separou os dois -> **extrator ancorado em forma sintatica quebra quando o codigo melhora**, e refatorar para melhor e exatamente quando ninguem suspeita do guarda. O conserto nao foi alargar a regex: o servico passou a exportar `APPEARANCE_KEYS` congelada, que e a autoridade viva, e o extrator le a lista. Codificado em `frontend/src/js/store/atlas-appearance.service.js` e na terceira forma de `extrairChavesDeSetting`.
- 2026-08-16 `verificacao-fantasma` [realidade] O dono escolheu "Plano", viu o mapa virar plano, deu F5 e o globo voltou. Eu tinha verificado a tela por captura e a logica por teste, e nenhum dos dois tocava o unico elo que falhava: a GRAVACAO. `saveAtlasAppearance` fazia `repo.getAtlas()` e, sem registro de Atlas, voltava sem gravar e sem dizer nada -- e nao haver registro e o caso COMUM, porque um slot local nasce com mapas e sem linha de atlas. Herdei o `if (!atlas) return` do modal antigo, o que significa que o exagero vertical nunca persistiu nesse ramo, desde sempre, e ninguem notou porque o valor sobrevivia em memoria ate o reload -> **efeito imediato mascara persistencia quebrada**: quando a UI aplica ao vivo, ver a mudanca acontecer nao e evidencia de que ela foi salva, e a unica prova e reler DO DISCO depois de um reload. O repositorio ja tinha `ensureAtlas()` para exatamente isto. Codificado em `frontend/src/js/store/atlas-appearance.service.js` (as duas escritas usam `ensureAtlas`) e no caso "grava mesmo quando NAO havia registro de atlas" de `frontend/tests/integration/remote-setting-op.test.js`.
- 2026-08-16 `teste-que-nao-prende` [realidade] Ao consertar o caminho remoto da mesma preferencia, achei `getControl('terrain')` no handler de op remota enquanto o registro usa `'TerrainControl'` (cinco chamadores de producao com o nome longo): o apply ao vivo NUNCA rodou para nenhum par. O guarda existia, `remote-setting-op.test.js`, e estava verde ha meses -- porque o dubla de `getControl` respondia ao nome ERRADO, o mesmo que o handler pedia. O dubla foi escrito copiando o codigo que ele deveria julgar -> **dubla derivado do sujeito confirma o erro do sujeito**, e a unica defesa e escrever o dubla a partir da AUTORIDADE (aqui, o nome que os chamadores de producao usam), nunca a partir da linha sob teste. O arquivo passou de 2 para 5 casos e agora nomeia o registro real. Codificado em `frontend/tests/integration/remote-setting-op.test.js`, com a razao escrita no cabecalho para o proximo leitor nao "simplificar" o nome de volta.
- 2026-08-16 `verificacao-fantasma` [realidade] O spec de "Fazer uma copia" escolhia o cartao do atlas com `hasText: /^Meu Atlas$/`, e o Playwright compara `hasText` contra o texto do ELEMENTO INTEIRO: o cartao carrega nome, "aberto ha ...", chip e nota, entao a ancora de linha nao podia casar em nenhum estado do produto. O caso esperou o timeout inteiro por um elemento impossivel e reprovou apontando para a copia, que estava certa -> **ancora de igualdade so mede o que voce ancorou; se o alvo e uma parte, mire na PARTE** (aqui o `.local-atlas__name` de dentro, via `filter({ has: getByText(nome, { exact: true }) })`), porque o vermelho de um seletor que nunca pode casar e indistinguivel do vermelho de uma funcionalidade quebrada e custa uma investigacao inteira no lado errado. O irmao dessa forma tambem ficou fechado no mesmo arquivo: `hasText: 'Meu Atlas'` casa por SUBSTRING e passaria a casar dois cartoes no instante em que a copia existisse, isto e, um locator ambiguo POR CONSTRUCAO que so falha depois, longe da causa. Codificado no helper unico `cartaoPeloNomeExato` de `frontend/tests/e2e-ui/copiar-atlas-local.spec.js`, com as duas armadilhas nomeadas no JSDoc dele e nenhum `hasText` de nome sobrando no arquivo.
- 2026-08-16 `verificacao-fantasma` [realidade] Depois de consertar o locator acima, o mesmo spec ficou verde e foi declarado pronto; medido EM SERIE, reprovava em 2 de 3 execucoes. A causa era um `allInnerTexts()` logo apos um `page.goto`: leitura UNICA, sem re-tentativa, contra uma lista que a pagina monta depois do boot, entao um retorno `[]` virava a mensagem "a copia sumiu" quando ela so ainda nao tinha sido desenhada -> **no Playwright, o que espera e a assercao (`expect.poll`, `toHaveCount`), nunca a leitura**, e todo `all*()` e uma foto do instante em que foi chamado. A licao de metodo e a que ja esta na constituicao e recorreu assim mesmo: um verde unico de coisa dependente de tempo nao se distingue do deterministico, e so a serie separa os dois. Codificado no `await expect(itens).toHaveCount(2, { timeout: 20000 })` que agora precede a leitura em `frontend/tests/e2e-ui/copiar-atlas-local.spec.js`, com o comentario dizendo por que ele nao e redundante com as assercoes seguintes.
- 2026-08-16 `contrato-implicito` [realidade] O primeiro desenho de "Fazer uma copia" de atlas local reusava o que ja existia: exportar o `.ebgeo` em memoria, criar o slot e IMPORTAR. O export estava certo e a copia chegava sem feicao nenhuma, sem um erro em lugar nenhum, porque o import nao-aditivo arrasta `clearAllDataStore` (e a memoria do atlas anterior, e a necessidade de recarregar a pagina para desfazer os dois): o wipe deixava um mapa `Principal` vazio ao lado do importado -> **reusar um pipeline importa os efeitos colaterais dele junto, e efeito colateral de pipeline nao aparece na assinatura da funcao que voce chamou**. O que fechou o caso foi descer de nivel: copiar os bancos chave por chave (`copyAtlasDatabases`) nao interpreta nada, nao monta nada e nao troca o atlas da aba, e a operacao de BAIXO nivel saiu menor que a de alto nivel que ela substituiu. Codificado no `@fileoverview` de `copyAtlasDatabases` e no JSDoc de `duplicateLocalAtlas` (`frontend/src/js/store/atlas-namespace.js`, `frontend/src/js/store/local-atlas.api.js`), os dois registrando a alternativa recusada por extenso para ninguem "simplificar" de volta para o round-trip.
- 2026-08-16 `premissa-inventada` [sessao] `copyAtlasDatabases` foi entregue SEM teste unitario, com a justificativa escrita de que ele "precisa de dois enderecos reais de IndexedDB". A justificativa era falsa desde 2026-08-15: `fake-indexeddb` e instalado como `setupFile` para TODO arquivo da suite, e dois enderecos reais e exatamente o que ele da -> **"nao da para testar" e uma afirmacao sobre o harness, e afirmacao sobre o harness se confere abrindo o harness**, nao se deduz da forma do sujeito. Custo do engano: a funcao mais destrutiva do lote (escreve em dez bancos) ficaria coberta so por um spec de navegador de 4 minutos. O controle negativo do teste novo mediu tres mutacoes e contou os vermelhos: direcao invertida 4 de 6 casos, fila de saida incluida na copia 3 de 6, recusa por `atlasId` em vez de endereco 1 de 6 (o caso que existe para isso). Codificado em `frontend/tests/unit/copia-de-atlas-local.test.js`, que roda sobre IndexedDB de verdade, nao mocka `localforage` de proposito (um `Map` nao distingue banco AUSENTE de VAZIO, que e o que prova a fila fora da copia) e declara no cabecalho o que ele NAO alcanca, para nao ser lido como cobertura completa.
- 2026-08-18 `verificacao-fantasma` [sessao] Os quatro censos (superficies de recurso nos dois pacotes, auditoria e papel global) tiravam o inventario de `git ls-files`, que enumera o INDICE: o arquivo escrito ha cinco minutos, que e justamente o que ninguem classificou ainda, ficava fora da varredura ate alguem dar `git add`, e o censo respondia verde sobre um conjunto que nao continha o trabalho novo -> **um guarda alimentado pelo versionamento fica cego exatamente onde o trabalho novo aparece, que e onde ele mais precisa enxergar**; a correcao e `--cached --others --exclude-standard`, e as duas metades (ve o nao rastreado, nao arrasta o ignorado) precisam ser MEDIDAS, porque a segunda e a que convida a `--others` sozinho. Codificado nos quatro arquivos, cada um com um caso que cria um `.js` nao rastreado e exige que o inventario o alcance E que a varredura o acuse: `backend/tests/unit/superficies-de-recurso-censo.test.js`, `backend/tests/unit/auditoria-censo.test.js`, `backend/tests/unit/papel-global-censo.test.js` e `frontend/tests/unit/superficies-de-recurso-censo.test.js`. Controle negativo rodado nos quatro (inventario revertido numa copia do arquivo: o caso novo reprova, e so ele).
- 2026-08-18 `verificacao-fantasma` [sessao] A varredura de cabecalho de cache do censo de superficies casava `/Cache-Control|setImmutableHeaders(/`, isto e, media PRESENCA: um cabecalho AUSENTE nao casa com nada e era invisivel para ela. Nao e hipotese, e o historico: era o estado pre-F9 das rotas JSON do 360, e continuou sendo o das listagens de catalogo e do payload aditivo de `/resource-access/visible` DEPOIS de a F9 ter fechado o 360 -- verde nas duas rodadas, porque a ausencia nao tinha como ficar vermelha. Medido fora de banda contra o codigo real: naquele estado a varredura de presenca achava ZERO sitios nos dois controllers (nada a classificar) e a nova acusa 3 de 3 -> **um censo que so ve o que existe nao pode cobrar o que falta; quem manda no conjunto tem de ser a lista de superficies, nunca a lista de achados**. Codificado como a varredura 4 de `backend/tests/unit/superficies-de-recurso-censo.test.js`: toda rota `recurso-com-filtro` declara seu regime, a declaracao e conferida contra o CORPO do handler nas duas direcoes (regime que o codigo nao cumpre E buraco que o codigo ja fechou), e o buraco so sobrevive como `sem-cabecalho-declarado`, com RISCO escrito e teto. Provado com fixture, em cinco pernas.
- 2026-08-18 `contrato-implicito` [realidade] O gate de ANEXAR recurso a um atlas pedia `manage` no atlas mais `assertCanSeeResource`, e `fn_can_see_resource` nao distingue NIVEL de concessao: quem tinha so `view` (o nivel cuja definicao literal e "ve e NAO repassa") emprestava o recurso ao atlas dele, enquanto `requireResourceShare` guardava essa mesma distincao na rota vizinha de reconceder. Somado ao `manage` que publica o atlas e ao `read` que um atlas `is_public` da a chamador ANONIMO, a soma de tres gates individualmente corretos entregava projeto 360 privado sem credencial nenhuma -> a licao nao e sobre um predicado errado, e sobre VOCABULARIO: quando um nivel de permissao existe para negar UMA acao ("repassar"), toda superficie que executa aquela acao com outro nome (aqui "emprestar") precisa consultar o MESMO gate, senao o nome novo e uma porta que ninguem lembrou de trancar. O conserto foi na porta de ENTRADA e nao na de saida, porque o `read` da saida e decisao registrada (R4) e mexer nele quebraria o visitante de link publico; e a consequencia extrema (anonimo em atlas publico alcanca o emprestado) passou a estar NOMEADA por extenso no JSDoc do gate, porque ela era o unico ponto do eixo que nao estava escrito em lugar nenhum. Codificado em `requireResourceRelay` (`backend/src/middleware/resource-access.js`, composto dos mesmos objetos de `requireResourceShare` mais `fn_can_produce_resource`, sem uma segunda copia da regra) e em `backend/tests/integration/atlas-emprestimo-repasse-autorizado.test.js`, cujo controle negativo mediu 5 vermelhos em 8 casos ao remover o gate; quatro arquivos de teste vizinhos tiveram a concessao de fixture TROCADA de `view` para `view_share`, nunca somada, porque fixture que segue comprando o mundo antigo faz a correcao parecer regressao.
- 2026-08-17 `teste-que-nao-prende` [realidade] Quatro controles negativos da fase de permissões vieram VERDES e nenhum devia: (a) a cláusula que o plano mandava testar na poda recursiva não muda nada com todos os elos vivos, porque o que ela faz é impedir que uma poda posterior re-date o que outra já derrubou, e nenhum caso tinha duas podas; (b) afrouxar a checagem de permissão DENTRO de `grantResource` ficava verde porque o middleware barrava antes, e a suíte inteira só chamava pela rota; (c) remover o papel novo da função de resolução ficava verde porque o valor era inalcançável até o `CHECK` da fase seguinte existir, ou seja, o guarda media um estado que o banco ainda não admitia; (d) o pior, reverter o predicado de privacidade do MVT do 360 passava verde, porque a suíte media privacidade na LISTAGEM e nunca no tile -> **os quatro têm a mesma forma e ela não é "faltou teste": é o teste medindo um caminho que não é o caminho protegido**, e um controle negativo binário ("ficou vermelho?") não distingue isso de cobertura boa. A abordagem que passou a valer é contagem: reverter o predicado e conferir que o NÚMERO CERTO de casos cai, porque um vermelho onde se esperavam oito é a única evidência que chega sozinha de que o censo mede o mesmo caminho várias vezes. Codificado em quatro lugares distintos: o caso de poda com duas podas encadeadas, `backend/tests/integration/resource-grants-escalonamento.test.js` chamando o SERVIÇO direto e não a rota, o `CHECK` da 018 entrando antes do guarda que depende dele, e casos que DECODIFICAM o MVT e conferem as duas camadas em `backend/tests/integration/sv360-privado.test.js`.
- 2026-08-17 `doc-sobre-codigo` [realidade] O plano da fase foi escrito lendo o repositório e errou em cinco pontos, todos descobertos na execução: o hard-delete do 360 mora em `sv360.admin.service.js` e não onde o plano dizia (e a ordem importa, porque a purga precisa ler as fotos antes do CASCADE); o plano fechava só a rota de LISTA de tilesets, enquanto `GET /api/v1/tilesets/:id` vazava a linha privada igualzinho; `COLS` em `catalog.service.js` é fixado em exatamente oito nomes por um teste de paridade, então a coluna nova exigia uma segunda constante e não um `COLS` maior; a página é `atlas.html` desde `0bbc3aee` e o plano dizia `projetos.html`; e a que de fato quebrou uma rota, o plano olhou só o `CHECK` de `action` da auditoria e não viu que `target_type` tem `CHECK` próprio e `target_id` era UUID, então gravar um slug levantava `22P02`, que chega ao cliente como um HTTP 400 sem relação aparente com o assunto -> **leitura não é execução, e um plano é prosa sobre o código com a mesma data de validade de qualquer outra prosa**; quanto mais detalhado o plano, mais fácil confundi-lo com o código. A regra que ficou: cada passo do plano se reconfirma contra o arquivo no momento de executá-lo, e divergência vira errata escrita, não correção silenciosa. Codificado como as cinco erratas na entrada de 2026-08-16 de [`decisions/decisions-2026.md`](decisions/decisions-2026.md) (o registro de decisão é o gancho aqui, porque não há teste que possa afirmar propriedade de um plano) e, para a última, na migração 020, que devolve o alvo à condição de coluna de primeira classe.
- 2026-08-17 `verificacao-fantasma` [realidade] Entreguei a soma dos recursos privados ao catálogo tendo exercitado UM caminho de sessão, o `login()`, e declarei a fase pronta. A restauração de sessão a partir do `localStorage` não somava nada, então quem tinha papel global ou concessão via o catálogo privado ESVAZIAR a cada F5, sem erro em lugar nenhum e sem teste vermelho -> **quando uma capacidade se pendura na sessão, a pergunta não é "funcionou?", é "por quantas portas a sessão nasce?"**; aqui são duas (gesto de login e restauração do disco), e a segunda é a que o usuário exercita mais, porque é a que roda em todo recarregamento. O sintoma tinha a assinatura clássica do defeito invisível: sumia depois de uma ação que ninguém associa à causa. Codificado em `frontend/tests/unit/recursos-privados-sobrevivem-f5.test.js`, guarda ESTRUTURAL (afirma que os dois caminhos chamam a soma, em vez de simular um deles), para que o terceiro caminho de sessão que alguém escrever nasça cobrado.
- 2026-08-17 `verificacao-fantasma` [realidade] A camada `e2e-ui` tinha porta do Vite, porta do backend, nome de banco e arquivo de estado FIXOS, e com dois checkouts do mesmo repositório na mesma máquina o `reuseExistingServer` do Playwright reaproveita o Vite que já estiver na 4321, servindo o `src/` do OUTRO checkout: a suíte mede código que não é o que se está editando, verde do começo ao fim. O banco é pior de outro jeito, porque a segunda rodada DROPA o banco da primeira no meio dela -> é a mesma classe que a constituição já nomeia ("o instrumento pode estar medindo outra cópia do sujeito", registrada para o `?t=` do HMR), e a recorrência mostra que ela não é sobre módulos, é sobre **toda coordenada compartilhada que o harness fixa em constante: porta, banco, arquivo de estado e diretório temporário são endereços, e endereço fixo tem um dono só**. Codificado nas quatro coordenadas aceitando variável de ambiente com os mesmos padrões de antes (`EBGEO_UI_E2E_APP_PORT`, `EBGEO_UI_E2E_BACKEND_PORT`, `EBGEO_UI_E2E_DB_NAME`, com o arquivo de estado derivando da porta do backend) e na seção "Duas cópias do repositório no mesmo computador" de `frontend/tests/e2e-ui/README.md`, que diz por extenso qual das quatro colisões é silenciosa.
- 2026-08-17 `teste-que-nao-prende` [realidade] Oito testes do 360 ficaram vermelhos quando o papel global passou a ser resolvido no BANCO, e a tentação foi tratá-los como regressão. Eles estavam CERTOS em reprovar: forjavam token de administrador com um `sub` que não tinha linha em `users`, e a propriedade que sumiu era exatamente "o token sozinho basta", que é o que o modelo novo deixou de aceitar de propósito, porque `flexibleAuth` não reconcilia e um papel revogado sobreviveria até 15 minutos dentro do JWT -> **fixture que compra uma propriedade que o produto está removendo faz a correção parecer regressão**, e a pergunta que separa os dois casos é qual asserção o vermelho está defendendo, nunca quantos arquivos ficaram vermelhos. O conserto foi criar a linha de usuário nas oito fixtures, não afrouxar a resolução. Codificado nas próprias oito, e a razão está no `@fileoverview` de `backend/src/middleware/resource-access.js`, que declara por que o papel nunca vem do token; o mesmo cuidado aparece na 021, em que quatro arquivos vizinhos tiveram a concessão de fixture TROCADA de `view` para `view_share` e nunca somada.
- 2026-08-17 `teste-que-nao-prende` [realidade] O auto-cadastro era uma escalação de privilégio (escolher a OM alheia num seletor entregava todo projeto 360 oculto e privado daquela OM), e ela estava **documentada por escrito num teste que a afirmava como comportamento correto**: `register-tenant-claim.test.js` tinha um caso cujo nome dizia que a declaração auto-feita COMPRAVA os projetos não publicados da organização. Um `KNOWN GAP` que assere o comportamento inseguro é a defesa certa contra risco esquecido, mas ele só funciona enquanto alguém lê o rótulo; passados meses, o que resta é um teste verde afirmando o vazamento como contrato -> **asserção que congela um risco aceito precisa QUEBRAR no dia em que o risco é fechado, e a única forma de garantir isso é a asserção mirar a consequência, não o mecanismo**. O conserto foi INVERTER aquele teste, não escrever um novo ao lado, porque deixar os dois faria a suíte afirmar as duas coisas. Codificado na inversão de `backend/tests/integration/register-tenant-claim.test.js`, no caso reescrito de `backend/tests/integration/register-organization-scope.test.js` (que mantém a metade que continua valendo, a lotação sem aprovação, e assere que a conta nasce SEM o crachá que autoriza) e no repro `backend/tests/integration/auto-cadastro-om-nao-autoriza.repro.test.js`.
- 2026-08-17 `funcionalidade-dormente` [realidade] `purgeResourceLinks` foi escrita na F5 (`e9bc3128`), com JSDoc explicando por que ela grava uma linha de trilha por vínculo apagado, e nunca foi ligada a nada: apagar um projeto 360 seguia deixando concessões e empréstimos órfãos vivos, apontando para um recurso que não existe mais. A única ocorrência do símbolo no repositório era a própria definição, e a suíte estava verde porque a função tem teste próprio -> **testar a função prova a função, nunca a fiação**, e o modo de falha aqui é o pior tipo, um buraco de integridade com auditoria por cima, que parece mais completo do que a ausência total teria parecido. A forma se detecta por contagem de ocorrências do símbolo, não por leitura: um export com uma ocorrência só é código morto até que se prove o contrário. Codificado na chamada dentro da transação de `deleteProject` (`backend/src/modules/streetview360/sv360.admin.service.js`) e no caso de integração que apaga o projeto e exige as concessões purgadas com a linha `PERMISSION_PURGE` na trilha.
- 2026-08-17 `doc-sobre-codigo` [realidade] Li o `CHECK` de `audit_trail.action` como inventário do que o sistema audita, e três valores (`LOGIN`, `LOGOUT` e `ATLAS_DELETE`) estavam declarados ali desde a 001 com contagem ZERO de emissores em `src/`. Quem filtra a trilha por `ATLAS_DELETE` recebe lista vazia e conclui que ninguém apagou atlas, que é a pior resposta possível, porque é indistinguível da verdadeira -> **declaração de schema é promessa, não evidência**; um valor de enum sem escritor tem exatamente o efeito de uma documentação desatualizada, com o agravante de parecer código. A generalização que ficou: todo vocabulário fechado (enum, `CHECK`, lista de tipos) precisa de um guarda que case DECLARADO contra EMITIDO, senão ele só cresce. Codificado em `backend/tests/unit/auditoria-censo.test.js`, que cobra emissor de toda ação declarada e mantém os buracos conhecidos numa lista com piso DECRESCENTE (allowlist que só pode encolher, para não virar barra de progresso esquecida), e nos emissores novos da 020.
- 2026-08-17 `verificacao-fantasma` [realidade] Dois flakes fecharam no caminho e nenhum era do produto, os dois do INSTRUMENTO: um caso de tab-lock decidia por relógio de parede e reprovava em 1 de 6 execuções (46 de 46 depois de ancorar na CHEGADA da mensagem, que é o evento que ele sempre quis medir), e `docs-integridade` estourava o timeout padrão de 5 s quando o cache de arquivo do sistema estava frio, porque ele lê cerca de 1300 arquivos, reprovando justamente no laço editar-e-verificar, que é quando o cache está mais frio -> **teste que mede tempo de parede mede a máquina, não o código**, e a lição do segundo é menos óbvia que a do primeiro: um timeout default é uma afirmação sobre o hardware de quem escreveu o teste, e quando o custo real do caso é I/O ele precisa de teto EXPLÍCITO com o número justificado, senão o vermelho aparece no pior momento e treina o leitor a re-rodar em vez de investigar. Codificado na âncora por chegada de mensagem no caso de tab-lock e no teto explícito de `frontend/tests/unit/docs-integridade.test.js`, com o comentário dizendo que o custo depende do cache de arquivo do SO.
- 2026-08-18 `verificacao-fantasma` [sessao] Rodei `npm test --prefix backend -- a.test.js b.test.js` e li o verde como veredito sobre os DOIS arquivos. O runner usa `args.find(a => !a.startsWith('--'))`, isto é, só o PRIMEIRO padrão que recebe, e o segundo arquivo nunca rodou -> é recorrência direta do bloco "conferir um subconjunto e tratar como o conjunto" (o mesmo comando já aparece no evento de 2026-07-24), e a recorrência prova o que a regra de PODAR prevê: **a prosa não segurou**, porque a advertência estava escrita em `backend/CLAUDE.md` e mesmo assim o comando saiu dos meus dedos com dois alvos. A abordagem muda de "lembrar" para "não conseguir": um alvo por comando, um comando por alvo, enquanto o runner não recusar o segundo padrão. Codificado por ora no bloco de comandos de `backend/CLAUDE.md`, que passou a mostrar o predicado exato e a dizer que já recorreu duas vezes; o gancho forte (o runner abortar quando receber mais de um padrão, como já faz com `--reuse-db` sem alvo) fica NOMEADO aqui como pendente, porque anotar de novo sem mudar o mecanismo é exatamente o que este arquivo manda não fazer.
- 2026-08-18 `teste-que-nao-prende` [sessao] O censo de superfícies cobrava piso de USO em duas classes (`publico-fixo` precisava de `>= 1` entrada, rota `recurso-publico-por-desenho` de `>= 2`), com a razão certa: classe vazia não discrimina nada. Ao fechar os bytes do `/assets3d`, as duas classes esvaziaram, e o piso passou a ter o efeito exatamente contrário ao que motivou escrevê-lo: para manter o censo verde alguém teria de **reabrir um buraco**, ou de deixar classificada como pública uma rota que já não é -> **piso de contagem é um proxy da afirmação que se quer fazer, e quando o trabalho remove o último habitante da classe o proxy vira incentivo perverso**; a saída não é baixar o piso (que enfraquece), é escrever a afirmação ESPECÍFICA que ele aproximava. Codificado em `backend/tests/unit/superficies-de-recurso-censo.test.js`: onde havia `>= 1` agora há "nenhuma decisão de cache do assets3d pode ser fixa" (com a contagem exata das quatro), e onde havia `>= 2` há uma igualdade nomeando `GET /api/config` como a única rota pública por desenho. Os dois novos reprovam regressão, que o piso não fazia.
- 2026-08-18 `premissa-inventada` [sessao] Escrevi um caso novo afirmando que TODA soletracao alternativa de um caminho privado tem de dar 404 ao anonimo, inclusive com `?atlasId=` do atlas publico que empresta o recurso. A rodada devolveu 200 e o 200 era a resposta CERTA: o visitante anonimo de um atlas `is_public` herda o emprestimo (decisao R4), entao a assercao pinaria um bug em vez de um contrato, e teria virado guarda contra o proprio produto. Escrevi a expectativa a partir do padrao do caso vizinho ("anonimo nao alcanca") em vez de derivar da regra de acesso -> **teste novo comeca pela regra, nunca pela simetria com o caso ao lado**, e a metade da propriedade que vale nos dois desfechos (o regime nunca pode voltar `public`) e mais forte que o status, porque nao depende de quem tem direito. Codificado no segundo laco de `backend/tests/integration/assets3d-privado.test.js`, que afirma `notEqual(cache-control, CACHE_PUBLICO)` em vez do status, com o comentario contando o erro.
- 2026-08-18 `verificacao-fantasma` [realidade] A suite do frontend reprovou uma vez em sete rodadas com ENOENT no arquivo temporario que o censo de superficies CRIA sob a pasta de fixtures e apaga dentro de um `finally` para provar que o inventario enxerga o nao rastreado. O Vitest roda arquivos em paralelo, e `docs-integridade` lista as fontes no load do modulo e as le dentro do caso: entre listar e ler, o vizinho apagou o arquivo. O comentario do censo afirmava que a fixture temporaria ficava "longe do inventario de qualquer outro guarda", e a afirmacao era falsa -> **arquivo temporario de um teste e estado global da suite**, e o guarda que enumera o repositorio precisa tolerar o que sumiu entre a listagem e a leitura, sempre para o lado estrito. Codificado no `catch (ENOENT) continue` de `frontend/tests/unit/docs-integridade.test.js`, provado por controle negativo (com o pulo, 9/9 verde com um arquivo listado que nao existe; sem ele, o mesmo ENOENT reaparece).
- 2026-08-18 `verificacao-fantasma` [realidade] A entrada do censo para `GET /atlas/:id/sync/:version` declarou um TETO duas vezes seguidas e errou por baixo nas duas: a primeira dizia "a entrega a todo membro" quando o alcance real era o visitante ANÔNIMO de link público, e a segunda, escrita já com essa correção, dizia que só restava a linha PRÉ-PREFIXO quando o ramo incremental da mesma rota entregava a definição em qualquer formato, o ATUAL inclusive. As duas vezes o teto foi escrito a partir do caminho que eu tinha acabado de instrumentar (o snapshot), e não de uma varredura dos ramos da rota -> **teto declarado é afirmação, e afirmação sem medição própria herda o recorte do conserto que a produziu**; uma rota com dois ramos precisa ser medida ramo a ramo antes de qualquer frase sobre o que ainda vaza, porque o ramo não instrumentado é justamente o que ninguém vai olhar de novo. Codificado na entrada reescrita de `backend/tests/unit/superficies-de-recurso-censo.test.js`, que agora nomeia os DOIS ramos e não declara teto nenhum, e nos dois pares de comportamento que a sustentam (`backend/tests/unit/catalog-layer-op-poda.test.js` e o caso do pull incremental em `backend/tests/integration/catalog-layer-cadeia-de-vazamento.test.js`).
- 2026-08-18 `premissa-inventada` [realidade] Escrevi duas expectativas de teste a partir da forma que eu lembrava, e as duas ficaram vermelhas na primeira rodada com o código CERTO do outro lado: que o pull incremental devolve o `entityType` do cliente (`catalogLayer`), quando `toFrontendOperation` devolve o `target` já normalizado (`catalog_layer`, uma assimetria real entre o relay e o replay); e que `GET /sync/1` traz a primeira op do log, quando `GET_OPERATIONS_SINCE_VERSION` é `server_version > $2` e a versão 1 pula exatamente a op número 1 -> **expectativa de teste se deriva do código que produz a resposta, nunca do envelope que se enviou**, e as duas vezes o atalho foi assumir simetria entre o que entra e o que sai. Codificado nos dois casos: o helper `deCatalogo` de `backend/tests/integration/sync-catalog-layer-privado.test.js` reconhece os dois vocabulários e o comentário diz por quê, e o caso do pull em `catalog-layer-cadeia-de-vazamento.test.js` ancora na versão corrente LIDA antes do push em vez do literal 1 (e mantém o pedido de `/sync/1` só como a varredura do log inteiro, que é o que ele mede bem).
- 2026-08-19 `verificacao-fantasma` [realidade] Provei a equivalencia do schema esmagado comparando `pg_get_functiondef` das duas bases, e o criterio que eu mesmo tinha escrito ("o lado novo nao pode ACRESCENTAR nada") ficou vermelho em duas funcoes. As duas diferiam SO no texto do comentario dentro do corpo, porque `pg_get_functiondef` devolve o corpo inteiro, prosa incluida, e eu havia reescrito aquela prosa para deixar de citar numeros de migracao que a consolidacao apagou. Concluir "e so comentario" lendo o proprio diff seria chancelar a minha saida -> **quando o instrumento mede codigo e prosa na mesma string, "e so prosa" nao e observacao, e hipotese, e ela precisa de um segundo instrumento que nao consiga ver prosa**. A saida nao foi congelar o comentario (o que anularia metade do proposito da consolidacao, que e justamente atualizar a prosa) nem afrouxar o criterio: foi uma secao NOVA no comparador, com os comentarios `--` removidos e o espaco normalizado, que tem de sair com ZERO acrescimos. Ela saiu com zero, e so entao a diferenca virou fato. Codificado como a secao L do harness da fase (descartavel, `backend/.er-scratch/`) e, de forma duravel, na errata da entrada de 2026-08-19 em [`decisions/decisions-2026.md`](decisions/decisions-2026.md), que registra o criterio corrigido para o proximo esmagamento.
- 2026-08-19 `teste-que-nao-prende` [realidade] Ao esmagar as migracoes, `EXCECOES_DESTRUTIVAS` ficou vazia por construcao, e o teste que a cobra faz `assert.equal(achados.length, EXCECOES.length)`: com as duas listas vazias isso vira `0 === 0` sobre uma varredura que nao achou nada, indistinguivel de uma regex quebrada. O caso continuaria verde para sempre, inclusive se `PADROES_DESTRUTIVOS` deixasse de casar qualquer coisa -> **lista de excecoes vazia nao e o fim do guarda, e o comeco do problema dele**: a comparacao que dava sentido a lista perde o poder de discriminar exatamente quando o trabalho a esvazia (mesma forma do piso de contagem que esvazia, ja registrada em 2026-08-18, e a recorrencia mostra que a classe e "proxy que perde o ultimo habitante"). Codificado no caso 'controle negativo' de `backend/tests/unit/migrations-higiene.test.js`, que roda os mesmos padroes contra uma amostra por padrao e exige deteccao, mais o par inverso (DDL aditiva nao pode disparar nenhum), com a contagem amarrada para que padrao novo sem amostra reprove. Provado de fora tambem: injetar um `DROP TABLE` num arquivo consolidado deixa o teste vermelho no caso certo, e so nele.
- 2026-08-19 `doc-sobre-codigo` [realidade] A consolidacao renomeou os oito arquivos de migracao, e as 66 citacoes na documentacao ficaram vermelhas de uma vez, como esperado, porque `docs-integridade` cobra caminho. As 41 citacoes em comentario de `.js` ficaram CALADAS, e uma delas ja estava podre havia meses: `sv360.merge.js` apontava para uma migracao que nunca existiu neste repositorio, residuo do PRIMEIRO esmagamento -> **a classe de referencia que ninguem vigia e a que apodrece primeiro, e o sinal de que ela existe e uma citacao morta sobrevivendo a um refactor inteiro**. Renomear sem fechar essa classe teria criado 41 mentiras silenciosas de uma vez. Codificado em `backend/tests/unit/citacao-de-migracao.test.js`, que extrai `NNN_nome.sql` de todo `.js` versionado de `src/`, `tests/` e `scripts/` e exige que cada um exista, com piso de discriminacao, allowlist para o nome fabricado de proposito por um controle negativo vizinho, e um caso que prova que o padrao pega a forma com sufixo de linha (que era a do residuo). O teste declara no cabecalho o que NAO alcanca: citacao por numero solto continua sem guarda, e por isso as 41 foram reescritas para citar arquivo ou SIMBOLO, nunca `arquivo:linha`.
- 2026-08-19 `regressao-propria` [sessao] Ao dar superficie ao grupo de acesso, acrescentei o beneficiario coletivo na ESCRITA (Joi, servico, `INSERT_GRANT`) e no predicado de LEITURA, e tratei o par como o conjunto. A tela "quem tem acesso" (`LIST_GRANTS_FOR_RESOURCE`) juntava o beneficiario em `users` por INNER JOIN, e numa concessao a grupo o `grantee_id` e NULL por CHECK: a linha inteira sumia da resposta -> **conceder devolvia 201 e a lista continuava vazia, sem erro em lugar nenhum**, que e o pior sintoma possivel numa tela de permissao, porque a pessoa reconcede e o efeito de cada tentativa e invisivel. A classe e a mesma "lista fechada" que a constituicao descreve para o nivel de permissao, na forma de JUNCAO: assumir que a coluna alternativa esta preenchida elimina a linha em vez de acusar. A licao pratica, que vale para o proximo tipo de beneficiario: alternativa nova numa coluna se varre pelos LEITORES da coluna irma, um a um, e nao pelo caminho que a escrita percorre. Codificado em `backend/tests/integration/resource-grants-grupo.test.js`, no caso "`GET /:type/:id/grants` LISTA a concessao a grupo, com nome e contagem de membros", que exige as DUAS linhas (a coletiva e a pessoal) na mesma resposta: so a coletiva deixaria passar um `LEFT JOIN` que perdesse a pessoal, e so a pessoal e exatamente o verde que o INNER ja dava.
