# Dependencies
node_modules/

# Environment
.env
.env.local

# Test environment (keep .env.test.example, ignore actual .env.test)
# .env.test is committed with safe defaults for local testing

# Runtime data
data/images/
data/test-images/
data/e2e-images/
data/assets3d/
data/sv360/
data/*.sqlite*

# Logs
*.log
logs/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Test coverage
coverage/

# package-lock.json é versionado (o `npm ci` do Dockerfile exige). A negação que
# morava aqui existia para escapar da regra `package-lock.json` do .gitignore da
# raiz, que não era ancorada e alcançava este diretório; essa regra foi removida.
yarn.lock
pnpm-lock.yaml
