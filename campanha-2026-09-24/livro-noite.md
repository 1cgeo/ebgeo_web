- 2026-09-23 `teste-que-nao-prende` [nuance-conhecida] `backend/tests/integration/cross-cutting-gaps.test.js` prendia como "known nuance" que republicar o link público devolvia a leitura ao token do link velho, ou seja, afirmava o defeito da cláusula 5.4 ("o link é revogável"). Consertado o produto na caça noturna, a rodada inteira do backend ficou vermelha exatamente ali. Codificado: o caso agora afirma o contrato (token velho recusado, token do link novo lê) e o repro `backend/tests/integration/link-publico-republicado-nao-revive-token.repro.test.js` está no "Preso por" da 5.4.
- 2026-09-23 `teste-que-nao-prende` [semente-que-a-ferramenta-nao-produz] Toda suíte de sync (o spec de todos os tipos, o e2e de contrato e o backend) semeava `processed_los`/`processed_visibility` com id UUID, que não é o id que a ferramenta produz; por isso nenhuma viu que o servidor recusava a saída da Linha de Visada e do Viewshed e que o par nunca a via. Codificado: `frontend/tests/integration/saida-de-analise-nao-viaja.test.js` e `frontend/tests/e2e-ui/browser-collab-analise-edicao.repro.spec.js`, que desenham com a ferramenta real.
- 2026-09-23 `teste-que-nao-prende` [teste-que-afirmava-o-defeito] Quatro testes existentes afirmavam o comportamento defeituoso e tiveram de mudar junto com o conserto: "drops system properties" (`frontend/tests/unit/user-data-atributos-de-importacao.test.js`, a perda do nome e das colunas reservadas na importação), "op pendente de OUTRO atlas" sem aviso (`frontend/tests/unit/multiaba-invariantes.test.js`, que só valia porque a fila daquele atlas era destruída), o `MAP_NOTES_REQUESTED` do inbound (`frontend/tests/integration/remote-operation-handler.test.js`, o pedido de ABRIR o painel na tela de todos) e o caso "mesmo registro" com `mapBadgeColors` (`frontend/tests/unit/late-legacy-plan.test.js`). Codificado: cada um passou a afirmar o contrato, com o repro do conserto ao lado; a pergunta "o que este verde provaria se o código estivesse errado" pega essa classe antes.
- 2026-09-24 `regressao-propria` [duas-frentes-no-mesmo-caminho] Dois consertos da mesma noite, cada um verde no próprio branch, se encontraram errado na integração: a rajada de criações remotas numa escrita só (desempenho) pulava a derivação da saída da visada (colaboração), e o repro do teto da cauda do pull conectava antes de haver conta, o que o conserto do cursor por principal passou a recusar. Codificado: o caso da visada em `frontend/tests/integration/rajada-remota-um-documento.repro.test.js` e a sessão antes do primeiro connect em `frontend/tests/integration/cauda-longa-vira-retrato.repro.test.js`; a lição de processo é rodar a suíte na INTEGRAÇÃO, não só em cada branch.
- 2026-09-23 `verificacao-fantasma` [teto-com-dist-velho] O commit da meteorologia declarou a suíte do frontend inteira verde medindo o teto de peso contra um `dist/` mais velho que o código; com build fresco o mapa media 4191 kB contra o teto de 4170, e esse vermelho cortava o `&&` da raiz antes do backend e do e2e. O crescimento tinha causa (dois imports estáticos traziam para o boot o texto dos painéis de ponto e o diálogo de compartilhar recurso). Codificado: os casos novos daquele teste prendem os dois imports; a regra de construir antes de ler a metade (b) continua sendo a mensagem do próprio teste.
